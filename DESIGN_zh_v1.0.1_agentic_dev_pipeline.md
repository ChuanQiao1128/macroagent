# MacroAgent · 产品设计文档（v1.0.1 执行收敛 + Agentic Dev Pipeline 版）

> **MacroAgent is a confidence-aware nutrition accounting agent for people who care whether their calorie deficit is actually real.**
>
> MacroAgent 是一个带置信度的饮食记账 Agent，帮助认真减脂 / 增肌的人判断自己的热量缺口到底是不是真的。

作者：Chuan Qiao · 版本：v1.0.1 · 更新：2026-05-03

---

## 零、战略核心（一句话）

> **Cal AI 卖的是「记录」。MacroAgent 卖的是「诚实的记账」。**

整个产品所有其他决策都从这句话展开。把这句话从设计文档的中段挪到第 0 章，是因为它不是修辞，它是产品的护城河来源：

| Cal AI | MacroAgent |
|---|---|
| `650 kcal` | `约 730 kcal · 范围 680–840 · 中等置信度` |
| `蛋白质 38g` | `蛋白质约 45g · 范围 38–48g` |
| 没有更多信息 | 主要不确定性：米饭份量（±170 kcal）、油/酱（±80 kcal） |
| 没有追问 | 一个高影响追问："米饭是半碗、一碗、还是一碗半？" |

这一个选择解锁了 Cal AI 结构性做不到的四件事：

1. **诚实的缺口核算** —— 用户能知道自己"500 kcal 缺口"是真的，还是可能在 -100 到 +300 之间
2. **定向追问** —— 只在「不确定性 × 卡路里影响」超过阈值时才打断用户
3. **个人化校准** —— 用户的每一次更正都让未来的不确定性收窄
4. **自我修正的复盘** —— 当一周体重趋势和记录的缺口对不上，系统能解释为什么

---

## 一、问题定义

### 1.1 真实痛点

任何尝试减脂或增肌的人，几乎都会撞到同一堵墙：

> "我今天到底有没有真的吃出热量缺口？蛋白质够不够？晚饭还能吃多少？"

这不是信息缺乏的问题，而是 **记录摩擦力 × 估算准确度 × 反馈滞后** 三重挤压的问题。绝大多数人坚持记录不超过 14 天，然后归因于"我代谢慢"或"减脂就是不科学"，事实却是：他们的输入数据本身就有 15–20% 的系统性低估（油、酱、隐形糖几乎不可能被肉眼或拍照识别）。

### 1.2 现有方案的失败模式

| 工具 | 做了什么 | 在哪里崩坏 |
|---|---|---|
| **Cal AI / SnapCalorie** | 拍照 → 一个 kcal/宏量营养素数字 | 把不确定性藏起来。一张照片看不见锅里那 15g 油、酱里的 8g 糖。"650 kcal" 看起来干净，实际常常 ±200 kcal，而你不知道。 |
| **MyFitnessPal / 薄荷健康** | 海量数据库 + 条码/手动搜索 | 摩擦力高。绝大多数用户两周内停用。 |
| **手动记账 + 表格** | 控制力最强 | 没人能持续。 |
| **找营养师** | 准确、个性化 | 贵，且不是实时的。 |

照片估算的天花板就是这样：AI 能识别"米饭 + 鸡肉 + 蔬菜"，但它看不见炒锅里的油、看不见酱料里的糖。把这些不可见因素 **折叠成一个数字** 是当前所有 AI 营养应用最大的设计错误，因为它让用户在一连串 +20%、+15%、+25% 的累计误差里得出错误结论。

### 1.3 真实饮食里最容易出错的不是识别，是这些

- 米饭 / 主食份量
- 油量
- 酱汁
- 外卖调料
- 饮料糖分
- 坚果 / 零食量
- 包装食品的实际 serving size

照片本身看不到这些。把这些不可见量 **显式暴露成不确定性** 才是产品哲学。

---

## 二、目标用户（ICP）

### 2.1 锐利定义

> **MacroAgent 面向已经知道 calorie deficit 和 protein target 很重要、但厌倦手动记录、又不相信 AI 单点估算的人。**

这类人不一定最多，但最容易理解我们的差异化。

### 2.2 谁不是我们的用户

- 只想随便看看热量的人
- 不愿意多点一下确认的人
- 只想要一个很干净数字、不在乎对错的人
- 对区间和置信度没有耐心的人

### 2.3 谁是我们的用户

- 已经用过 MyFitnessPal / Cal AI / 薄荷健康并放弃的人
- 认真减脂或增肌的人
- 关注蛋白质摄入的人
- 对数据有信任需求、不接受黑盒的人
- 愿意每天多花 5 秒换更可靠判断的人
- 重复性饮食结构高的人（80% 的饭来自 20% 的菜单）

典型画像：25–40 岁，技术 / 知识工作者，已经尝试过至少一款营养 App 并放弃，对"AI 黑盒一个数字"持怀疑态度。

---

## 三、产品的四个真正优势

### 优势一：不是「更准」，而是「更诚实」

承诺不是"我每顿都比 Cal AI 准"，而是：

> 我会告诉你这个数字有多不确定、为什么不确定、以及哪个问题最值得问。

这比"我比别人准"更可信，因为它不依赖打不赢的视觉精度战，而是依赖一个 Cal AI 的产品哲学决定它做不了的事——**承认不确定性**。

### 优势二：把「追问」做得非常克制

很多健康 App 失败是因为太烦。MacroAgent 不能变成"请填米饭克数 / 请填鸡肉克数 / 请填油量"。正确策略是：

> **一次最多问一个问题，而且只问对结果影响最大的那一个。**

例：米饭份量影响 ±170 kcal、油量影响 ±80 kcal、蔬菜份量影响 ±15 kcal —— 系统只问米饭。这是产品的关键平衡：

- Cal AI：完全不问，体验顺滑，但隐藏误差
- MyFitnessPal：什么都要填，准确但没人坚持
- **MacroAgent：只问一个最有价值的问题**

### 优势三：天然有「个人记忆」护城河

Cal AI 主要是通用模型体验。MacroAgent 越用越准，因为它能记住：

- 你的常规早餐
- 你的 flat white 通常用什么奶
- 你的蛋白粉品牌
- 你的常用饭碗大小（"那个黑色碗的一碗 ≈ 220g"）
- 你的鸡肉饭通常多少米饭
- 你周末常漏记零食
- 你外卖通常低估油量
- 你经常把一碗半米饭说成一碗

这些不是公共数据库能解决的。这是 MacroAgent 的长期护城河：**不是模型越来越聪明，而是系统越来越懂你。**

### 优势四：Multi-Agent 是必要的，不是表演

这个项目有真正独立的决策点：

1. 这是热菜、饮料、包装食品还是条码商品？
2. 视觉识别不确定时该信哪个候选？
3. 数据库冲突时信 USDA、Open Food Facts、还是个人历史？
4. 份量不确定要不要问用户？
5. 今天看似有 deficit，但隐藏油模式下还成立吗？
6. 晚餐怎么吃才能补蛋白又不超热量？

**Agent 负责不确定性决策，Tool 负责确定性计算。** 这比"我做了很多 agent"高级得多。

---

## 四、产品原则

1. **Honest > Confident**：宁可给区间，也不给假精确。
2. **Ask only when it matters**：默认不打扰；只问一个高影响问题。
3. **Trace > Trust**：每个数字都能点开看到来源（USDA / 个人历史 / 条码 / 食谱拆分）。
4. **Local-first**：V0–V1 数据本地存放，用户的饮食历史是 ta 自己的资产。
5. **System gets sharper with use**：每一次更正都写入个人 RAG 语料；越用越准是承诺，不是噱头。
6. **诚实 ≠ 复杂**：界面默认极简，技术细节按层级展开；不能让用户因为"诚实"而觉得难用。

---

## 五、核心体验：三层输出

> 区间、置信度、不确定性虽然诚实，但可能让用户觉得复杂。所以 UI 不能一上来就给一堆技术信息。**输出必须分三层。**

### 第一层：用户最关心的结论（默认显示）

```
午餐已记录
约 730 kcal
蛋白质约 47g
置信度：中等
```

这里 **保留一个 best estimate**（不是只给区间），让用户立即拿到行动信息。

### 第二层：诚实区间（一眼可见的小字）

```
范围 680–840 kcal
蛋白质 42–52g
```

提示用户"不要过度相信单点数字"。

### 第三层：为什么不确定 + 一个追问（轻点展开 / 主动卡片）

```
主要不确定性：
1. 米饭份量    ±170 kcal
2. 油 / 酱汁   ±80 kcal

米饭大概是？
[半碗] [一碗] [一碗半]
```

也就是说，每一餐的输出都是：

> **best estimate + range + confidence + one question**

既有 actionability，也有 honesty。

---

## 六、核心用户流（Happy Path）

```
1. 用户拍下午饭照片。

2. 系统在 ~5 秒内回复：
   "午餐：约 730 kcal（680–840 范围），蛋白质约 47g（42–52 范围），中等置信度。
    主要不确定性：米饭份量（±170 kcal）。
    一个问题：半碗 / 一碗 / 一碗半？"

3. 用户轻点"一碗"。

4. 系统更新当日：
   "今天已摄入：约 1,440 / 2,100 kcal（区间收窄到 1,420–1,470），
    蛋白质 83 / 150g。
    热量：在轨。蛋白质：还差 67g。
    建议晚餐：450–600 kcal，40g+ 蛋白质。"
```

### 6.1 边缘流（Edge Cases）

| 场景 | 系统行为 |
|---|---|
| 拍照模糊 / 看不清 | 不强行猜测，要求重拍或语音补充 |
| 识别为饮料 / 零食 | 直接走条码或预设品类，不走「拍照→拆分」 |
| 用户漏拍一餐 | 在晚饭时给出"是否补记午餐"的低摩擦入口 |
| 外食 / 聚餐 | 提供"按菜系大致估"的低置信度模式，不假装精确 |
| 用户更正 | 写入个人语料；同类菜下次默认置信度提高 |

---

## 七、为什么需要 Multi-Agent（不是单次 LLM 调用）

单次 vision LLM 调用就是 Cal AI 在做的事。它只在「单一菜品 + 干净背景」勉强可用，一旦进入真实饮食场景就会塌陷，因为系统必须 **独立做出多个判断**：

| 决策点 | 单次调用为什么不行 |
|---|---|
| 这是条码商品、热菜、还是饮料？ | 路由到完全不同的数据源 |
| 视觉不确定时哪个候选食物对？ | 需要个人历史 + 文本交叉验证 |
| 这个份量不确定值不值得问用户？ | 需要计算 `不确定性 × kcal 影响` 并阈值化 |
| 多个数据库冲突时信谁？ | USDA / OpenFoodFacts / 个人记录可能差 30% |
| 今天的缺口在隐形油模式下还成立吗？ | 需要跨餐反思，不是单次推理 |
| 晚饭剩余宏量该怎么吃？ | 是开放性建议决策，不是记录决策 |

---

## 八、Agent 设计：MVP 只做 4 个

> 文档 v0.2 列了 6 个 Agent。MVP 不需要全做满。**砍到 4 个，把 Coach 和 QAReflection 推迟到 V1。**

### 8.1 Agent 1 · OrchestratorAgent

判断输入类型并路由：照片 / 文字 / 语音 / 条码 / 用户修正 / 每日复盘。

```json
{
  "route": "meal_photo",
  "next_agent": "MealUnderstandingAgent",
  "reason": "Input contains a meal image."
}
```

### 8.2 Agent 2 · MealUnderstandingAgent

做视觉理解，**不直接算热量**，只拆组件 + 给出 portion hint 与 uncertainty。

```json
{
  "components": [
    {
      "name": "white rice",
      "confidence": 0.72,
      "portion_hint": "medium bowl",
      "uncertainty": "bowl depth unclear"
    },
    {
      "name": "chicken",
      "confidence": 0.81,
      "portion_hint": "one palm-sized portion"
    },
    {
      "name": "vegetables",
      "confidence": 0.68
    }
  ]
}
```

**关键：Vision Agent 不算最终 calories，只拆组件。** 把估算工作交给后续可追溯的 RAG。

### 8.3 Agent 3 · FoodMappingRAGAgent

把组件映射到营养数据。优先级：

1. 个人膳食记忆
2. 个人食材库
3. Open Food Facts
4. USDA FoodData Central
5. 菜谱拆分知识库

```json
{
  "component": "white rice",
  "macro_entry": "USDA cooked white rice",
  "source": "USDA",
  "kcal_per_100g": 130,
  "protein_per_100g": 2.7,
  "confidence": 0.85
}
```

### 8.4 Agent 4 · PortionAndAccountingAgent（最关键）

做三件事：

1. 根据 portion range 算 calories / protein **区间**
2. 判断是否需要追问（`不确定性 × 影响 > 阈值`）
3. 更新当天账本

```json
{
  "meal_kcal_range": [680, 840],
  "meal_kcal_best": 730,
  "protein_range": [42, 52],
  "confidence": "medium",
  "question": {
    "needed": true,
    "reason": "Rice portion uncertainty may change estimate by 170 kcal.",
    "text": "米饭大概是多少？",
    "options": ["半碗", "一碗", "一碗半"]
  }
}
```

### 8.5 推迟到 V1 的两个 Agent

#### QAReflectionAgent（周级复盘）

需要 7–14 天数据才有意义。它解决：

> "你记录显示每天 deficit 500 kcal，但体重 14 天没变。原因是什么？"

可能给出：TDEE 估高 / 外卖低估 / 周末漏记 / 油被长期低估 / 水分波动。

#### CoachAgent（下一餐建议）

输入剩余 calories + 蛋白缺口 + 用户偏好 + 常吃食物 + 时间。输出 2–3 个候选餐方案，**必须基于 accounting 结果**，而不是独立做聊天建议。

### 8.6 Agent / Tool 边界（再次诚实）

| 组件 | 类型 | 为什么 |
|---|---|---|
| Orchestrator | Agent | 真路由决策 |
| MealUnderstanding | Agent | 置信度感知的组件抽取 |
| FoodMappingRAG | Agent | 多源仲裁 |
| PortionAndAccounting | Agent | 阈值决策 + 是否追问 |
| 宏量计算器 | **Tool** | 纯算术 |
| 日总和器 | **Tool** | 纯聚合 |
| 图表渲染 | **Tool** | 模板 |
| 条码查询 | **Tool** | API |

**MVP：4 个真 Agent + 4 个工具。** 不把简单算术包装成 AI。

> **Agent 与 LangGraph 节点的映射关系**：第 30 章 LangGraph 实现里把上面 4 个逻辑 Agent 进一步拆成 8 个状态节点。
> 其中 PortionAndAccountingAgent 拆成 `account_portions` + `decide_question` + `apply_answer` 三个节点，因为它们各自是可独立测试 / 可缓存 / 可恢复的状态转移。
> 「逻辑 Agent」是设计语言，「图节点」是工程实现 —— 两个层级都不必牺牲。

---

## 九、RAG 的三个真正作用

> 这个产品的 RAG 不是普通问答 RAG。它有三个作用，缺一不可。

### 作用一：食物映射

```
flat white       → 用户常喝的 trim milk flat white
white rice       → USDA cooked white rice
protein yoghurt  → Open Food Facts barcode item
鸡肉饭           → 用户历史中的 chicken rice bowl
```

### 作用二：个人校准

用户修正"这不是半碗，是一碗半"，写入：

```json
{
  "context": "black bowl rice portion",
  "correction": "user selected one and a half bowl",
  "default_weight_g": 240,
  "confidence_boost": 0.15
}
```

下次类似照片：rice portion range 从 150–280g **收窄到 210–260g**。

### 作用三：菜谱拆分（复合菜）

对于 Pad Thai / 宫保鸡丁 / 炒面 / 火锅 / 咖喱饭 / poke bowl / burrito 这类菜，**RAG 不应该返回一个单点 kcal**，而应该返回：

```json
{
  "dish": "fried noodles",
  "components": ["noodles", "oil", "vegetables", "meat", "sauce"],
  "hidden_calorie_risks": ["oil", "sauce"],
  "default_confidence": "low",
  "recommended_question": "这是外卖还是自己做的？"
}
```

### 9.1 冲突仲裁规则

- 条码命中 > 任何视觉 / 名称匹配
- 个人记忆 > USDA（如果命中相似度 > 0.85）
- 多源加权平均时，**保留分歧大小作为不确定性的一部分**

---

## 十、系统架构

```
                          ┌──────────────────────┐
   照片 / 语音 / ────────►│  OrchestratorAgent  │
   条码 / 文字            └──────────┬───────────┘
                                     │ 按 intent + confidence 路由
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
       ┌──────▼─────────────┐   ┌────▼───────────┐    ┌─────▼────────────────┐
       │ MealUnderstanding  │   │ FoodMappingRAG │    │ PortionAndAccounting │
       │ Agent              │   │ Agent          │    │ Agent                │
       │ 图片→食物组件      │   │ 组件→宏量+来源 │    │ 区间 + 追问决策      │
       │ 不算 kcal          │   │                │    │ + 当日账本           │
       └────────────────────┘   └────────────────┘    └──────────────────────┘
                                                                   │
                                          ┌────────────────────────┤
                                          │                        │
                                  ┌───────▼────────┐    ┌──────────▼──────┐
                                  │  TraceUI       │    │  V1: CoachAgent │
                                  │  展示每个决策  │    │  V1: QAReflect  │
                                  │  的来源        │    │                 │
                                  └────────────────┘    └─────────────────┘

知识库（RAG corpus）:
  • personal_meal_memory     • personal_pantry
  • open_food_facts          • usda_food_data
  • recipe_decomposition     • user_correction_log
```

---

## 十一、数据模型

```python
FoodComponent {
    name: str
    visual_confidence: float
    candidates: list[Candidate]
    estimated_grams: tuple[int, int]
}

MacroEntry {
    food_id: str
    source: enum(PERSONAL, PANTRY, OPENFOODFACTS, USDA, RECIPE_KB)
    source_url_or_ref: str
    kcal_per_100g: float
    protein_per_100g: float
    carbs_per_100g: float
    fat_per_100g: float
    source_confidence: float
}

Meal {
    id: uuid
    timestamp: datetime
    photo_path: str | None
    components: list[(FoodComponent, MacroEntry)]
    kcal_range: tuple[int, int]
    kcal_best: int
    protein_range: tuple[int, int]
    overall_confidence: enum(LOW, MEDIUM, HIGH)
    user_correction: dict | None
    asked_question: str | None
    answered: str | None
}

DailyLedger {
    date: date
    target_kcal: int
    target_protein_g: int
    consumed: list[Meal]
    kcal_consumed_range: tuple[int, int]
    protein_consumed_range: tuple[int, int]
    deficit_status: enum(REAL_DEFICIT, AMBIGUOUS, REAL_SURPLUS)
}
```

**所有 macro 字段都同时携带 best estimate 和区间。** 这是数据层对"诚实 + 可行动"的承诺。

---

## 十二、技术栈

| 层 | 选择 | 理由 |
|---|---|---|
| LLM（视觉 + 推理） | Claude Sonnet | 多模态精度 + JSON 结构化输出稳定，同栈减少 prompt 维护成本 |
| Embedding | `text-embedding-3-small` 或本地 bge-small | 个人语料量小，本地够用 |
| 向量库 | Chroma（V0–V1）→ pgvector（V2） | 起步低成本，可平滑迁移 |
| 后端 | Python + FastAPI | 与 Agent 编排兼容性最好 |
| Agent 编排 | LangGraph（仅用于 MealAnalysisGraph 主流程） | 可恢复状态 + human-in-the-loop + node-level trace |
| 工具层 | 普通 Python（macro 计算、账本、条码、OCR） | 确定性、可单测、可解释，不引入框架不必要的复杂度 |
| 数据持久化 | SQLite（V0–V1）→ Postgres（V2） | 本地优先 |
| 前端 | V0 Streamlit；**V0.5 React + Tailwind Web Demo**；V1 起 React Native | 先满足作品集/招聘信号，再进入移动端真正可用 |
| Eval / Observability | 自建 golden set + DeepEval/Ragas + LangSmith/Helicone traces | 每次 prompt 改动跑回归；线上看成本、延迟、失败链路和用户更正 |

---

## 十三、视觉估算的技术深度（核心竞争力）

> 「诚实记账」是产品哲学，但护城河必须建在**真正能把照片估准**这件硬事上。
> 用户不会因为"我们承认不确定性"留下来，会因为"我们的区间真的很窄、追问真的很值"留下来。
> 本章是工程深度的承诺，不是技术栈的复述。

### 13.1 估算误差链 —— 知道误差在哪里才能砍它

一张照片到最终 kcal 中间有 5 个误差源，按贡献排序：

```
1. 份量误差   (±25–40%)  ← 最大头
2. 烹饪方式   (±15–30%)  ← 隐藏油 / 酱
3. 食物识别   (±10–20%)  ← 鸡胸 vs 鸡腿
4. 营养密度   (±5–15%)   ← USDA vs 实际品种
5. 重量→kcal (±0%)       ← 纯算术
```

**份量是头号杀手。** 我们的工程精力 60% 应该砸在份量估计，30% 在烹饪方法识别，10% 在食物识别本身。这是产品级的优先级判断。

### 13.2 Tier 1 · 软件能解决（V0–V1）

#### A. 餐具识别 + 容量数据库

```
检测到：白色陶瓷碗（标准饭碗 直径 ~12cm）
匹配到：常见米饭碗容量库 → 满碗 ~250ml ≈ 200g 米饭
+ 视觉判断填充度："约 80% 满"
→ 估算 160g（区间 145–180g）
```

自建一个 **餐具尺寸库**：常见碗/盘/杯（中餐饭碗、汤碗、马克杯、星巴克杯型、便当盒、外卖塑料盒）每种 50 个标准尺寸。这是 **prompt 里的硬知识**，不靠模型瞎猜。

#### B. 参照物自动检测（隐式比例尺）

照片里几乎总有可作为比例尺的物体：
- **餐具**：勺子（汉勺 ~15cm、西餐勺 ~17cm）、筷子（24cm）
- **手**：成年人拇指 ~6cm、手掌 ~10×18cm
- **常见物**：iPhone（已知尺寸）、信用卡（85.6mm）
- **品牌包装**：可乐罐（330ml = 直径 6.6cm × 高 11.5cm）

视觉模型识别这些 → 反推盘中食物的实际尺寸。**用户什么都不用做。**

#### C. 个人餐具校准

第一次拍到家里某个碗：

```
🍚 这是一个新的碗，能帮我标定一下它的容量吗？
   [装满水/米的照片，30 秒]
   或者直接：[这是 400ml 大碗] [250ml 中碗] [180ml 小碗]
```

写入个人语料：

```json
{
  "object_signature": "user_black_ceramic_bowl_v1",
  "visual_hash": "sha:...",
  "capacity_ml": 280,
  "calibrated_at": "2026-05-01"
}
```

之后**所有用这个碗的照片**自动套用容量。一次校准，终身受益。

#### D. 烹饪方法视觉线索 + 强制追问

视觉模型按线索打标：
- **油光面积** → 煎/炸/红烧概率
- **焦色梯度** → 烤/煎程度
- **汤汁颜色** → 酱料浓度（暗示糖/盐）
- **盘底油渍** → 炒菜油量

不确定时强制追问：

```
看起来是煎过的？
[煎/烤]  [水煮/清蒸]  [炒]  [其他]
```

**烹饪方式选过一次，写入对应菜的个人偏差因子**。下次同菜不再问。

#### E. 多组件分割（不是一个 bbox）

主流 calorie app（包括 Cal AI）经常把一盘菜当成"一个食物"出 kcal。我们必须做**逐项 mask**：

```
照片
  → 视觉模型输出每个组件的 mask + 像素面积
  → 每个组件单独走 RAG + 份量估算
  → 加总到这餐
```

**这一步直接决定了"米饭份量 ±170 kcal"这种细粒度归因能不能做出来。** 整盘黑盒的话，我们的核心卖点直接崩。

#### F. 菜系特化 prompt

同一个模型，**prompt 按菜系分流**：

```python
if cuisine_hint in ["中餐", "亚洲菜"]:
    prompt += CHINESE_FOOD_RULES
    # "注意识别隐藏油（炒菜锅气）、酱料糖分、米饭碗深度"
elif cuisine_hint in ["日式"]:
    prompt += JAPANESE_FOOD_RULES
    # "注意酱油钠含量、味噌汤、寿司饭醋糖"
elif cuisine_hint in ["西式"]:
    prompt += WESTERN_FOOD_RULES
```

菜系判断由 Orchestrator 做（基于地理位置 / 时间 / 用户历史）。

### 13.3 Tier 2 · 利用手机硬件（V1.5–V2）

#### A. iPhone LiDAR 体积测量

iPhone 12 Pro 起的 LiDAR 能直接给出**毫米级深度图**。一张 LiDAR 照片 = 食物的实际体积，不再靠视觉猜碗深。

```
LiDAR 输出：米饭区域体积 ~165 ml
→ 米饭密度 ~1.25 g/ml
→ 重量 ~206 g（不是区间 150–280g 了！）
```

**这一项能把份量误差从 ±25% 降到 ±5%。** 投入产出比极高，仅在 Pro 机型可用是接受成本。

#### B. 多角度 / 短视频代替单张照片

3 秒短视频自然包含俯视 + 侧视：

```
用户绕餐盘拍 3 秒 → 我们抽 5 帧 → 3D 重建近似体积
```

不如 LiDAR 准，但适用于所有手机。**默认就是视频，不是单张照片。**

#### C. AR 实时估算覆盖层

打开相机 → 实时叠加：

```
[实时取景]
  └─ 米饭：约 180g · 240 kcal
  └─ 鸡肉：约 110g · 180 kcal
  └─ 总计：约 420 kcal · 中等置信
  
拍下确认 ↓
```

**估算在拍照前就出现**——用户能看到 AI 在"看"什么，体验感极强。

#### D. 「吃前 + 吃后」双照片

```
点击"吃完了"按钮 → 提示："拍一张剩下的"
  → 视觉对比 before/after
  → 减去剩余 = 实际摄入
```

外食浪费场景特别有用："这道菜你吃了 70%，剩 30% 打包"。

### 13.4 Tier 3 · 模型层优化（V2+）

#### A. 食物专用 vision 模型微调

通用 LLM 在标准菜上够用，但中餐 / 复合菜 / 外卖经常错。

路径选择：
- **不自己训练**（V0–V2）—— 代价 >> 收益
- **prompt-engineering + few-shot** 把 USDA/中餐高频菜的"标准长相"塞进 system prompt
- **V3+ 才考虑** 用我们自己的标注数据 fine-tune 一个小模型（比如 Qwen2-VL）替代部分通用调用

#### B. 双模型校验（高价值菜专用）

复杂菜（火锅、麻辣烫、自助餐）一张照片同时给 Claude + Gemini 两个模型，**对结果做交叉验证**：
- 一致 → 高置信度
- 不一致 → 低置信度 + 追问用户
- 成本翻倍但只用在 ~10% 高不确定餐次，整体成本可控

### 13.5 Tier 4 · 用户配合（V1.5+）

#### A. 首次校准模式（开屏 5 分钟）

新用户激活的第一件事：

```
🎯 5 分钟校准
帮我准准你的常用容器，之后每张照片估算更准。

[1/5] 你的常用饭碗
       ┌──────┐
       │ 拍照 │ → 我会问你它装多少水
       └──────┘

[2/5] 你的常用马克杯
[3/5] 你的常用大盘
[4/5] 你做菜常用的勺子（量勺/汉勺）
[5/5] 你的常用便当盒
```

5 张照片 + 5 个数字（或 5 次"装水称重"），**之后 80% 的家庭餐饮自动有标定容器**。这是新手 onboarding 的高价值步骤——而且只需要做一次。

#### B. "更正即学习"的快捷路径

任一餐条目长按 → 滑动条改份量 → 系统：

```
你把"鸡胸饭"的 kcal 从 580 调到 720（+24%）
→ 个人偏差因子 +22% 已应用到这道菜
→ 同时反推：你常用的"煎鸡胸"可能比 USDA 高 30%（油量大）
[同意，全部应用]   [只改这一餐]
```

**用户的每一次更正不只改一条记录，是在训练系统。**

### 13.6 视觉估算的专属 Eval

通用 Eval（已在第 14 章）不够。视觉估算需要独立的硬指标：

| 指标 | 目标 V0 | 目标 V1 | 目标 V2 |
|---|---|---|---|
| 食物识别 IoU（mask 重叠率） | > 0.65 | > 0.75 | > 0.85 |
| 份量重量绝对误差 | < ±35% | < ±20% | < ±10%（含 LiDAR） |
| 区间覆盖率（真值落区间内） | > 75% | > 85% | > 92% |
| 区间宽度中位数 / best estimate | < 35% | < 25% | < 15% |
| 烹饪方式正确率 | — | > 70% | > 85% |
| 个人餐具校准后误差降幅 | — | > 30% | > 50% |

**Golden Set 必须包含中餐 / 外卖 / 复合菜**，不能只是 USDA 标准菜照片。这是西方竞品的盲区，我们必须主动覆盖。

### 13.7 Tier 优先级 —— 什么时候做什么

| Tier | 内容 | 阶段 | 投入产出 |
|---|---|---|---|
| Tier 1 A/B/E/F | 餐具库、参照物、组件分割、菜系 prompt | V0–V1 必做 | ⭐⭐⭐⭐⭐ |
| Tier 1 C/D | 个人餐具校准、烹饪方法追问 | V1 | ⭐⭐⭐⭐⭐ |
| Tier 2 A | iPhone LiDAR | V1.5（仅 Pro 机型） | ⭐⭐⭐⭐⭐ |
| Tier 2 B | 短视频代替照片 | V1.5 | ⭐⭐⭐⭐ |
| Tier 2 C | AR 实时叠加 | V2 | ⭐⭐⭐ |
| Tier 2 D | 吃前/吃后双照片 | V2 | ⭐⭐⭐ |
| Tier 3 A | 食物专用模型微调 | V3+ | ⭐⭐ |
| Tier 3 B | 双模型校验 | V2 | ⭐⭐⭐ |
| Tier 4 A | 5 分钟校准 onboarding | V1 必做 | ⭐⭐⭐⭐⭐ |
| Tier 4 B | 更正即学习 | V1 必做 | ⭐⭐⭐⭐⭐ |

### 13.8 一句话承诺

> **用户每用一周，我们的估算就准一档。**
> 不是因为模型变强了，是因为：
> - 你的餐具被标定了（容量精确）
> - 你的烹饪方式被记住了（油量精确）
> - 你的食物偏差因子被反推了（密度精确）
> - 你的常吃菜走快速通道（识别精确）
>
> **三个月后，你的"白米饭"在我们这里比在任何竞品都准。** 这是 Cal AI 一年也追不上的工程深度。

---

## 十四、版本路线图

### V0（7 天必须发版）

> 目标不是做 App，是 **证明核心逻辑**：区间 + 一个追问，是否真的比单点 calorie 输出更有价值。

```
✓ 拍照 → Claude Vision → FoodComponent[]（不做完整 Agent 编排）
✓ 50 条 USDA 食物 + 20 条作者手动添加的常吃食材
✓ 卡路里 / 蛋白质区间 + best estimate 输出
✓ 每餐 JSON 落盘
✓ Streamlit 单页：今日 kcal + 蛋白 + 剩余
✓ 公开 GitHub 仓
✓ 作者本人连续 1 天端到端记录每一餐
✓ 2 分钟演示视频
```

**V0 显式不做**：完整移动 App、完整条码库、体重趋势、Coach、复杂 recipe DB、订阅、运动消耗、社交。

### V1（V0 上线 + 7 天真实数据后）

按优先级：

1. **PortionAndAccountingAgent 完整版 + 追问 UI** —— 当我们知道哪些食物从照片最容易判错
2. **个人膳食记忆** —— 高频餐走快速通道
3. **FoodMappingRAGAgent + 混合检索** —— 当我们知道哪些通用 DB 条目失败
4. **CoachAgent** —— 当数据量足够定义"好建议"是什么
5. **QAReflectionAgent + 体重对账** —— 至少需要 14 天数据

### V1.5（菜库即资产 —— 见第二十章详细设计）

- **解锁式产品节奏**（Day 8 / 15 / 30 / 60 解锁不同 AI 能力）
- **「我的菜库」独立 tab**
- **下周菜单建议**（基于个人菜库重组）
- **时间敏感推送**（早 8 点、午 11:30、晚 22:00）
- **冰箱食材匹配**、外食地图、菜库进化时间线

### V2（产品化）

- React Native 移动端
- 多用户 / 云同步（端到端加密）
- 与 Apple Health / Google Fit 双向同步
- 教练模式：可被营养师订阅查看的脱敏面板（B2B2C）

---

## 十五、评估框架（6 类指标）

> 不要只看"识别准不准"。这个产品要看 6 类指标。

### 类别 1 · 记录摩擦

- 平均每餐记录时间 → 目标 ≤ 15 秒（V0），≤ 10 秒（V1）
- 每餐平均追问次数 → 目标 ≤ 1
- 用户是否连续记录 7 天 → 目标 7 日留存 > 50%

### 类别 2 · 估算质量（核心）

- **热量区间是否覆盖真实值（interval coverage）→ 目标 > 80%**
- 蛋白质区间是否覆盖真实值
- best estimate 与真实值的中位绝对误差

> 因为我们卖的是 honest accounting，**区间覆盖率比单点误差更重要**。这是与 Cal AI 评估方式的根本不同。

### 类别 3 · 追问价值

- 系统问的问题是否真的缩小了区间？
- 用户回答后 uncertainty 是否下降？
- 每个问题带来的 kcal range reduction → 目标平均 ≥ 100 kcal

例：米饭问题让区间从 680–840 缩小到 710–760，**就是好问题**。

### 类别 4 · 个人记忆价值

- 用户常吃餐再次出现时，追问次数是否下降？
- 置信度是否提高？
- 估算区间是否变窄？
- 目标：**同类餐出现 3 次后，追问次数下降 50%**

### 类别 5 · 当天目标价值

- 用户是否知道今天蛋白质缺口？
- 是否按建议补蛋白？
- 晚餐建议采纳率

### 类别 6 · 体重趋势一致性（V1）

- 记录的 deficit 与 7/14 天体重趋势是否一致？
- 系统能否发现长期低估模式（例：周末平均低估 ~300 kcal）？

---

## 十六、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **用户不喜欢区间，只想要单点** | 核心赌注被否 | 主卡片显示 best estimate 单点，区间放副信息，不确定性放详情。"诚实"以分层 UI 实现，不强加给用户 |
| **产品显得比 Cal AI 麻烦** | 流失 | 一次最多一个问题；只点按钮不打字；不让用户输入克数；不让用户查 DB |
| **拍照漏报严重** | 数据稀疏 → Coach 没数据用 | 提供"补记"入口 + 周复盘提醒 |
| **视觉模型识别错（鸡腿认成鸡胸）** | 蛋白估算错 30% | 高不确定时强制走候选选择，不静默猜 |
| **LLM 调用成本失控** | 单元经济崩坏 | 缓存 + 小模型路由简单决策；条码 / 常见菜走规则避开 LLM |
| **隐私顾虑（饮食习惯敏感）** | 用户拒装 | 本地优先 + 端到端加密（V2） + 不开广告变现 |
| **RAG 数据源冲突 30%+** | 区间无意义地宽 | 个性化语料权重逐步提升；条码与个人记忆优先 |
| **Cal AI 复制"区间"** | 差异化变窄 | 真正护城河是个人语料 + 反思 Agent，不是 UI |
| **健康风险（涉及饮食障碍 / 极低热量）** | 法律 + 道德 | Guardrails：不鼓励极低热量、不鼓励长期过大 deficit、对未成年人谨慎、对疑似饮食障碍倾向给出安全提醒；CoachAgent 必须受规则约束；明确不是医疗建议 |

---

## 十七、商业模式（早期假设）

- **免费层**：每天 3 餐 + 基础区间
- **Pro 层（$6.99 / 月）**：无限餐 + Coach + 周复盘 + 跨设备同步
- **不卖广告**。健康类应用卖广告会立刻把"诚实"变成谎言。
- **B2B2C（V2+）**：营养师 / 健身教练订阅，看其客户的脱敏面板

LTV/CAC 阈值：单用户 6 个月内 > 3x。如果留存做不到 V0/V1 目标，砍掉付费功能、做开源工具。

---

## 十八、最终差异化（vs Cal AI）

| 维度 | Cal AI | MacroAgent |
|---|---|---|
| 输出 | 单点 calories | calories range + best estimate + confidence |
| 不确定性 | 隐藏 | 显示来源和影响 |
| 追问 | 通常不追问 | 只问一个高影响问题 |
| 数据来源 | 模型 + 数据库 | RAG + source + confidence |
| 个性化 | 有一定历史 | 以 personal calibration 为核心 |
| 目标 | 快速 logging | honest deficit accounting |
| 长期价值 | 记录方便 | 越用越准 + 体重趋势反思 |

---

## 十九、UI 设计差异（vs Cal AI 实测对比）

> 我们和 Cal AI 在 UI 框架上**几乎一样**——都是首页 + 相机 + 历史 + 手动添加。差异不在像素层，而在**每一个数字背后的语义**。本节基于 Cal AI 真实截图逐屏对比。

### 18.1 先承认 Cal AI 做对的事

- 首页极简：一个大数字 + 三个 macro 圆环，信息密度刚好
- 相机三种模式（扫描食物 / 条形码 / 食品标签）路径分流是对的——这正是我们说的"包装食品走另一条路"的 UI 体现
- 多入口低摩擦：手动 / 语音 / 已保存食物 / 扫描
- 餐条目用「照片 + 名称 + 卡路里」卡片，认知成本低

**视觉设计层面 Cal AI 已经做得很好，我们不在「美观度」上和它竞争。** 真正的差距全在「每个数字背后有没有 confidence、source、可追问性」。

### 18.2 首页大数字的改造

| 位置 | Cal AI | MacroAgent |
|---|---|---|
| 大数字 | `2127 剩余卡路里` | `2127 剩余 ± 95` + `[✓ 缺口大概率为真]` |
| 展开（下拉） | 无 | `今日缺口 -260 kcal · 范围 -180 到 -350` + `数据可信 · 7 餐已记录 · 0 餐缺失` |

默认保持 Cal AI 风格的极简，**长按或下拉展开**才看到完整的 honest accounting。再次贯彻"诚实分层、不强加"。

### 18.3 三个 Macro 圆环的改造

Cal AI 只有"剩余多少"，没有"该不该补"。

```
Cal AI:
  172g 蛋白质剩余
  22g  碳水剩余
  27g  脂肪剩余

MacroAgent:
  蛋白 ⚠ 还差 67g · 已摄入 83/150g · 建议晚餐补 40g+
  碳水 ✓ 充足   78/100g 剩余
  脂肪 ✓ 充足   43/70g  剩余
```

把"剩余数字"变成 **「该不该 take action」的判断**——这是 CoachAgent 的雏形，首页就能体现 hint。

### 18.4 餐条目卡片的改造（带「来源 pill」）

Cal AI 的卡片只有名字 + 卡路里，没有来源 / 置信度信息。

MacroAgent 在每条记录后加一个 **小图标 / 颜色 pill**：

| 来源 | 图标 | 颜色 | 含义 |
|---|---|---|---|
| 条码 | 🏷 | 绿 | 精确，标签数据 |
| 营养标签 OCR | 📋 | 绿 | 精确，标签数据 |
| 个人记忆 | 🧠 | 蓝 | 高置信度，已记过 N 次 |
| 视觉 + USDA | 📷 | 黄 | 中等置信度，是估算 |
| 视觉，未确认 | ❓ | 灰 | 低置信度，请补充 |

例子：
```
氧碎蛋白瘦条 · 巧克力花生
170 kcal · 18g 蛋白
🏷 条码精确 · OXYSHRED 官方营养表
```

**点开任一条目能看到「为什么是这个数字」**——展开来源、份量假设、不确定性来源。Cal AI 结构上加不了这个，因为它的数据层根本没保留这些。

### 18.5 相机界面的改造

Cal AI 的三个模式直接抄过来，已经很对了。但加两点：

**1. 智能默认模式**：摄像头取景实时识别——看到条码自动建议扫条码，看到营养标签自动建议 OCR，看到一盘菜默认走"扫描食物"。**不要让用户去想该选哪个模式。**

**2. 加第四模式「🎤 描述」**：适合"我刚吃完，没拍照"的场景。语音说"两片披萨 + 一瓶可乐"，让 Orchestrator 走文字流水线。Cal AI 把语音藏在手动添加里，使用率会很低。

### 18.6 手动搜索界面的改造

Cal AI 建议条目就一个数字，没有来源、没有份量歧义提示。

MacroAgent：

```
建议（基于你常吃的）：
  🧠 Peanut Butter（你常用的牌子）
     188 kcal · 2 tbsp · 你上次的份量
     [+]

  📋 Avocado
     130 kcal · 半个 (≈100g) · USDA
     [选份量 ▾]   ← 一定要让用户选份量

  📋 Egg
     78 kcal · 1 个 · USDA
     [选份量 ▾]
```

关键改动：
1. **优先显示个人记忆**（带 🧠 图标），用户一眼看到"这是你上次的"
2. **份量必须可调**，不要默认"1 serving"一锤子定音
3. **来源标识**保持一致（🧠 / 📋 / 🏷 / 📷）

### 18.7 长按更正（Cal AI 完全没有的能力）

任一餐条目 → 长按 → "更正"：
- 改份量 / 改食物 / 改成另一品牌
- 修改写入 `user_correction_log` → 同类菜未来默认置信度提高 + 区间收窄

这是「越用越准」在 UI 层面的承诺兑现。

### 18.8 一张表收尾

| 位置 | Cal AI | MacroAgent |
|---|---|---|
| 首页大数字 | `2127 剩余` | `2127 剩余 ± 95 · ✓ 缺口为真`（详情可展开） |
| Macro 圆环 | "剩余 172g" | "⚠ 蛋白差 67g，建议晚餐补 40g+" |
| 餐条目卡片 | `170 kcal` | `170 kcal · 🏷 条码精确` |
| 浓缩咖啡 + 糖 | `3 kcal`（漏算糖包） | **`19 kcal`（追问后确认糖包）** |
| 视觉估算的菜 | `730 kcal` | `约 730 · 范围 680–840 · 📷 中等置信` |
| 相机模式 | 用户手动选 | 智能默认 + 加「🎤 描述」 |
| 手动搜索 | 单一数字 | 个人记忆优先 + 必选份量 + 来源标识 |
| 错误反馈 | 改不了 | 长按 → 更正 → 写入个人语料 |

> Cal AI 的每个数字都是"AI 估算的最终结论"。
> MacroAgent 的每个数字都是"best estimate + range + source + 一个可追问的不确定性"。
>
> UI 看起来 80% 像，但用户在我们这里能做的事（追问、更正、看来源、收窄区间、个性化）Cal AI 一件都做不了——**不是因为 Cal AI 团队懒，而是因为它的数据层从一开始就把这些信息扔了。**

---

## 二十、最强 Demo（三幕）

### Demo 1 · 「那包白糖」—— 杀手 demo 🔪

**场景**：用户拍下午茶——一杯 espresso + 旁边一包 white sugar（典型咖啡馆场景）。

**Cal AI 实测输出**：

```
浓缩咖啡  ·  3 卡路里
```

它**完全没算那包糖**。一包 white sugar ≈ 4g 糖 = **16 kcal**。
真实值是 **19 kcal，误差 533%**。

更隐蔽的杀伤力：用户不会发现。3 和 19 都"小到可以忽略"。但如果用户每天 3 杯加糖咖啡，**一周漏记 ~330 kcal**——足以把"500 kcal deficit"变成"180 kcal deficit"。这就是文档第一章说的「系统性低估」的真实样子。

**MacroAgent 输出**：

```
识别到：
  ☕ Espresso     3 kcal
  🍬 white sugar 包  ← 被检测到了

一个问题：
  这包糖加进咖啡了吗？
  [加了]  [没加，是装饰]  [一半]
```

用户点"加了" → 总计 **19 kcal、4g 碳水**。

→ **这是产品最强的一张对比图。**「Cal AI: 3 kcal · MacroAgent: 19 kcal · 因为它看到了那包糖。」

### Demo 2 · 「区间收窄」—— 米饭份量

上传一张午餐图（米饭 + 鸡肉 + 西兰花）：

**Cal AI 风格**：
```
730 kcal · 47g protein
```

**MacroAgent**：
```
约 730 kcal · 范围 680–840 · 中等置信度
蛋白质约 47g · 范围 42–52g

主要不确定性：
  - 米饭份量 ±170 kcal
  - 油 / 酱汁 ±80 kcal

一个问题：米饭是？  [半碗] [一碗] [一碗半]
```

用户点"一碗"：
```
更新：约 735 kcal · 范围 710–760
蛋白质 45–50g
```

→ **直观展示「区间 + 一次点击 = 误差砍半」。**

### Demo 3 · 「个人记忆」变准

第一次拍 flat white：
```
系统问：这是 whole milk / trim milk / oat milk？
```
用户选 trim milk。

第二次再拍：
```
你的常规 trim milk flat white
约 120 kcal · 7g protein · 高置信度（无追问）
```

→ **直观展示「越用越准」是承诺不是噱头。**

### Demo 4（彩蛋）· 全天 deficit 真实性

晚上：
```
今天你大概率仍有 250–420 kcal deficit。
但蛋白质差 36–48g。

建议晚餐：400–550 kcal、40g+ protein。
候选：① 希腊酸奶 + 蛋白粉  ② 鸡肉卷  ③ 金枪鱼沙拉 + 烤面包
```

→ **直观展示「目标价值」与 Coach 的克制。**

---

## 二十一、V1.5 路线图：菜库即资产（产品定位升级）

> **心智转换**：从"为了减肥忍受拍照" → "通过拍照建立你的个人饮食图谱，顺便减肥"。
> 这是 Cal AI 永远跑不通的产品哲学，也是 V1.5 的统一主题。

### 20.1 核心机制

```
拍一张照片 ← 不是浪费时间
            ↓
       存进你的菜库
            ↓
   菜库越大 → 解锁越多 AI 能力
```

拍照的意义被根本改写：每一张照片都在为用户**建一本属于自己的菜谱书**。

### 20.2 解锁式产品节奏（核心创意）

不是廉价勋章，是真实价值的解锁，每次都让用户感受到"再用 N 天能解锁更强的 AI"。

```
Day 1–7   · 起步期
   功能：基础区间记录、追问、来源追溯
   解锁中：你的菜库 (3/20 道)

Day 8–14  · 第一次「值了」时刻 ⭐
   解锁：📅 第一份"下周菜单建议"

Day 15–30 · 模式期
   解锁：🎭 你的饮食人格画像
   解锁：🔁 自动相似餐识别（"早餐 v1" 模板）

Day 30–60 · 个性化期
   解锁：⚖️ 个人热量偏差因子（你的"白米饭"实际是 158 kcal）
   解锁：🌡️ 能量曲线预测

Day 60+   · 教练期
   解锁：🎯 下单前预测
   解锁：💬 AI 教练对话（基于完整数据）
   解锁：📊 月度 vs 月度对比
```

### 20.3 「我的菜库」独立 tab

不只是后台数据，是用户能浏览、欣赏、整理的空间：

```
🍱 我的菜库 · 37 道

按频率：🔥 周冠军 / ⭐ 月冠军
按场景：☀️ 早餐 (8)  🍱 午餐 (15)  🌙 晚餐 (11)  🍿 加餐 (3)
按角色：💪 高蛋白 (12)  🌾 主食 (8)  🥗 轻食 (7)
按耗时：⚡ <10 分钟 (5)  🍳 <30 分钟 (18)  🍲 慢炖 (4)
按场所：🏠 在家 (24)  🏢 公司 (8)  🍴 外食 (5)
```

→ 用户第一次看到自己饮食的全貌，是一种"被看见"的体验。

### 20.4 下周菜单建议（Day 8 高光时刻）

满 7 天后第一次推送：

```
🎉 你的第一份个性化菜单已生成

下周 21 餐全部基于你这 7 天吃过的 18 道菜重组
预计达到：每天 -300 kcal · 蛋白 145g

周一：
🌅 早餐：你的"燕麦+蓝莓+flat white"（已吃 5 次）
🍱 午餐：你的"鸡胸饭"（已吃 3 次）
🌙 晚餐：你的"三文鱼+藜麦+牛油果"（已吃 2 次，能补足蛋白缺口）

[换一道]   [全周一览]
```

### 20.5 「冰箱有这些，能做啥？」

```
你说还剩：鸡胸 200g · 鸡蛋 4 个 · 西红柿 2 个 · 米饭

📚 从你菜库里匹配到 3 道：
  ① 鸡胸番茄炒蛋 + 米饭（你做过 4 次）
  ② 鸡胸沙拉 + 煎蛋（你做过 2 次）
  ③ 鸡蛋盖饭（你做过 1 次）
```

→ 解决"今晚吃啥"的决策疲劳。RAG 你的菜库 + 食材匹配。

### 20.6 餐厅版菜库 ——「外食地图」

```
🍴 我的外食地图 · 12 家店

附近 1km：
  ☕ 星巴克 → 你常点 5 种，平均 320 kcal
  🍜 张师傅面 → 你常点 3 种（清汤牛肉性价比最高）
  🥙 Subway → 你常点 1 种"鸡肉三明治+生菜+番茄+酸奶酱"

今天午餐想：
  💪 补蛋白 → Subway 那款（38g 蛋白）
  🍜 来碗暖的 → 张师傅清汤牛肉
  ⚡ 快 → 星巴克鸡肉卷
```

→ Cal AI 永远做不到的"外食个人化"。亚洲市场的关键功能。

### 20.7 时间敏感推送

不是被动查询，是主动在你需要时出现：

| 时刻 | 推送内容 |
|---|---|
| 早 8:00 | "通常你这点已吃完早餐。[早餐 v1] [早餐 v2] [今天换换]" |
| 午 11:30 | "今天午餐打算吃啥？基于剩余 macro：[3 个菜库选项]" |
| 晚 18:00 | "今天蛋白还差 50g，推荐你菜库里这 3 道高蛋白晚餐" |
| 晚 22:00 | "今天就这样了 ✓ 蛋白达标 92%、deficit -380。明天给你推早餐建议。" |

→ 从"用户主动打开 App"变成"App 在对的时刻出现"。

### 20.8 菜库进化时间线

3 个月后给用户看自己饮食的演变：

```
🌱 2 月初（菜库 5 道）·  随机外卖为主
🌿 3 月中（菜库 22 道）· 早餐 v1 固定
🌳 4 月底（菜库 37 道）· 3 套早餐模板 / 5 套午餐 / 8 道高蛋白晚餐

📊 客观结果：
  · 蛋白：78g/天 → 142g/天
  · 体重：-3.2 kg
  · 记录稳定性：30% → 91%
```

→ Cal AI 数据没有"故事"结构，做不到这件事。

### 20.9 V1.5 设计原则

1. **冷启动期诚实**：Day 1–7 明确告诉用户"AI 在收集你的菜库，第 8 天会有惊喜"。不画大饼。
2. **解锁是真价值，不是积分**：每次解锁的功能本身好用。
3. **AI 永远只推你吃过的菜**：菜库才 5 道也不胡乱推荐。
4. **菜库是用户的，不是 App 的**：永远能导出（PDF 菜谱书 / JSON）、本地优先。
5. **不强加多样性**：你想 30 天吃同一个早餐 = OK，AI 不评判。

### 20.10 V1.5 vs Cal AI / MacroFactor 对比

| 维度 | Cal AI | MacroFactor | MacroAgent V1.5 |
|---|---|---|---|
| 拍照的意义 | 记录今天 | 记录今天 | **给自己建图书馆** |
| 留存机制 | 自律 + 焦虑 | 算法调目标 | **解锁式正反馈** |
| AI 推荐 | 通用菜谱 | 不推菜 | **只推你吃过且喜欢的** |
| 决策疲劳 | "今晚吃啥"永远是问题 | 不解决 | **菜库 + AI 推 = 1 秒决策** |
| 外食 | 数据库 lookup | 数据库 lookup | **你的个人外食地图** |
| 时间维度 | 数字消失了 | 体重曲线 | **3 个月看饮食演变** |
| 退订后 | 数据消失 | 数据消失 | **菜库可以印成 PDF 菜谱书** |

### 20.11 V1.5 优先实施顺序

1. **解锁节奏 + Day 8 第一份周菜单** —— 最强留存机制
2. **「我的菜库」独立 tab** —— 让用户随时看到自己的资产
3. **时间敏感推送** —— 把 App 从被动变主动

---

## 二十二、V2.0 路线图：Pre-bite AI（决策前介入）

> **所有竞品都是 post-meal accounting —— 吃完了才记账。**
> 没有一家在你吃之前帮你做决定。
> Pre-bite AI 是 V2.0 的核心战略：把产品从"卡路里计算器"升级为"卡路里教练"。

### 22.1 核心机制

```
传统流程（所有竞品）：
   想吃什么 → 吃 → 拍照 → 看数字 → 后悔 / 满意

MacroAgent V2 流程：
   想吃什么 → 拍菜 → AI 分析 → 决定吃多少 / 换什么 → 吃 → 自动确认记账
```

**决策从被动事后变成主动事前。** 这是产品形态的根本升级。

### 22.2 三种 Pre-bite 场景

#### 场景 A · 餐厅菜单优化器

到餐厅 → 拍菜单照片：

```
检测到 28 道菜。
你说说今天的需求：
  💪 想吃肉    🥗 想清淡    🍜 想暖    ⚡ 要快    🎉 不在乎，今天放松
```

用户选"想吃肉"，AI 排序：

```
基于你今天剩余 macro（500 kcal、80g 蛋白）：

🟢 推荐
  ① 黑椒牛柳    520 kcal · 38g 蛋白 · 完美匹配
  ② 清蒸鱼      380 kcal · 32g 蛋白 · 蛋白略低，加碗汤补

🟡 谨慎
  ③ 宫保鸡丁    隐藏油 + 高糖酱，实际可能 700+ kcal
  ④ 红烧肉      你今天脂肪已超

🔴 不推荐
  ⑤ 糖醋里脊    糖摄入今日已达上限
  ⑥ 蛋炒饭      和你之前的午餐重叠，碳水超标
```

→ **菜单变成排序好的购物车。** 用户照着推荐点。

#### 场景 B · 上桌后的 Pre-bite 评估

菜上桌、还没动筷：

```
[拍餐桌全景]

🤖 这桌 5 道菜 + 米饭，3 人分。
   按你过往"3 人聚餐你大概吃 30%"的模式估算：
   
   你这一顿大概会摄入：800–1200 kcal、35–48g 蛋白
   
   今晚剩余目标：500 kcal、80g 蛋白
   
   建议：
   ✓ 多吃：清炒虾仁（高蛋白）、白灼菜（无油）
   ⚠ 少吃：糖醋排骨（糖+油）、麻婆豆腐（油）
   ❌ 最好不碰：今晚的甜点
   
   [接受建议]   [今天放松，不算了]
```

→ **在动筷前 30 秒决定怎么吃**。比吃完后悔有用 100 倍。

#### 场景 C · 外卖下单前预测

打开美团 / 饿了么前先和 AI 聊：

```
我想点麻辣香锅 + 米饭 + 一杯奶茶

🤖 预测：
  · 这餐 1,400–1,700 kcal（占你 TDEE 70%）
  · 蛋白质 35g（你今天还差 67g，这餐贡献不够）
  · 钠 ~3,800 mg → 明早体重大概率 +0.5–1.0 kg（基于你过去 8 次类似餐的实际反应）
  · 23:00 大概率饥饿（脂肪+碳水高、蛋白低，胃排空快）

替代方案：
  · 香锅减一半米饭 + 加一份豆腐 → 多 20g 蛋白 + 少 400 kcal
  · 奶茶换无糖 → 少 250 kcal

[按原方案点]   [按建议改]   [完全放弃，给我别的推荐]
```

→ **这是基于你过去这家店真实结果的预测**，不是泛泛营养学。RAG 调用你的"这家店"历史记录。

### 22.3 为什么这是 V2 不是 V1

Pre-bite 需要的前置条件：
1. **个人菜库够大**（≥ 60 道）—— 否则替代方案推不出
2. **个人偏差因子已建立**（V1.5 阶段）—— 否则预测不准
3. **餐厅历史记录够多**（≥ 5 家店各 ≥ 3 次）—— 外食预测才有据
4. **AR / 实时拍摄能力**（V1.5 Tier 2 已铺路）

V0–V1.5 是**积累**Pre-bite 所需的数据基础。V2 是**收割**。

### 22.4 Pre-bite 的差异化（vs 所有竞品）

| 时机 | Cal AI | MacroFactor | Foodvisor | MacroAgent V2 |
|---|---|---|---|---|
| 吃之前 | ❌ | ❌ | ❌ | ✅ **决策支持** |
| 吃中间 | ❌ | ❌ | ❌ | ✅ 桌前评估 |
| 吃之后 | ✅ 记账 | ✅ 算法调目标 | ✅ 教练计划 | ✅ + 个人化偏差因子 |
| 第二天 | ❌ | ✅ 体重对账 | ❌ | ✅ + 归因到具体菜 |

**所有竞品的时间窗都在"吃之后"。MacroAgent 是唯一在"吃之前"就介入的产品。**

### 22.5 关键 UX 原则

1. **Pre-bite 不能是新增 Tab，必须是相机旁边的第二快捷入口**——降低使用摩擦
2. **3 秒内必须给出建议**——不然用户已经点完了
3. **提供"今天放松"按钮**——不让用户被 AI 绑架
4. **建议必须基于个人历史**——任何看起来像"普适营养学"的建议都失败
5. **建议接受后自动记录**——不让用户做"先 pre-bite 再记账"两步操作

### 22.6 商业价值

Pre-bite 是**留存 + 付费转化**双杀器：

- 用户每次外食前打开一次 = **每周 ~10 次粘性触点**
- 替代方案推荐 = 显性"AI 帮我省了 400 kcal" = **付费意愿激增**
- 预测准确率随个人数据增长 = **越用越离不开**
- 同时是 **B2B2C 入口**：营养师可以通过看用户 pre-bite 决策记录做更好的咨询

### 22.7 一句话承诺

> **Cal AI 在你后悔时给你看一个数字。MacroAgent 在你犹豫时给你一个决定。**
>
> 这是从"卡路里 logging app"升级为"AI 营养教练"的关键一跃。

---

## 二十三、V0 → V1 工程任务清单

> 把前面所有设计转化成可直接建 GitHub Issues 的任务卡。每张卡片包含 title、estimate、依赖、acceptance。
> 优先级：**P0 = 必做，P1 = 可推后但同版本要做，P2 = 下一版本**。

### 23.1 V0（7 天发版）任务清单

#### EPIC 1 · 视觉拆解流水线（P0，估时 1 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-001 | Claude Sonnet 4.6 vision 接入 + JSON Schema 强制 | 10 张测试图全部返回合法 JSON |
| TASK-002 | 图片预处理（压缩 1024 长边、HEIC→JPEG、EXIF 旋转） | 任何来源照片都能正常入参 |
| TASK-003 | 图片 hash → 结果缓存 | 重传同图 < 50ms 命中 |

#### EPIC 2 · 食物映射 + 营养计算（P0，估时 1.5 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-010 | 50 条 USDA seed（米/面/鸡/牛/鱼/蛋/奶/果蔬） | JSON 入库可查 |
| TASK-011 | 20 条作者个人食材 seed | 包含品牌奶/蛋白粉/酸奶 |
| TASK-012 | 名称匹配（fuzzy + alias 表） | 中英混输入命中率 > 90% |
| TASK-013 | 区间计算器（portion × macro → kcal_range） | 单测 100% 覆盖 |
| TASK-014 | best estimate（区间几何中位） | — |

#### EPIC 3 · 持久化（P0，估时 0.5 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-020 | SQLite schema（Meal / FoodComponent / MacroEntry / DailyLedger） | 迁移脚本可重放 |
| TASK-021 | JSON 备份导出脚本 | 一键导出全部数据 |

#### EPIC 4 · Streamlit 单页 UI（P0，估时 1.5 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-030 | 上传图 → 显示识别 + 区间 | 端到端 < 6 秒 |
| TASK-031 | 今日 dashboard（已摄入 / 目标 / 剩余） | 区间和单点同时显示 |
| TASK-032 | 餐次列表（最近上传） | 含来源 pill |
| TASK-033 | 用户更正（最朴素版本） | 改份量后区间更新 |

#### EPIC 5 · 部署 + 文档（P0，估时 0.5 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-040 | 公开 GitHub repo + README | README 含一键运行 |
| TASK-041 | 2 分钟演示视频（用「白糖包」demo） | 上传 GitHub Releases |
| TASK-042 | 作者本人连续记 1 整天 | 发现的 bug 全开成 issue |

#### EPIC 6 · V0 自检 Eval（P0，估时 1 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-050 | 20 张作者真实餐照片 + 手动称重真值 | Golden set v0 入库 |
| TASK-051 | 端到端跑 → 计算区间覆盖率 | 目标 > 75% |
| TASK-052 | 失败模式分类 → 写入 V1 backlog | 至少 5 类 |

**V0 总估时：约 6 天。第 7 天发版 + 录 demo。**

---

### 23.2 V1（V0 + 7 天真实数据后）任务清单

#### EPIC 7 · 完整 4-Agent 编排（P0，估时 3 天）

| # | 任务 |
|---|---|
| TASK-100 | OrchestratorAgent（照片 / 文字 / 条码 / 修正回答路由） |
| TASK-101 | MealUnderstandingAgent（独立 prompt + JSON Schema） |
| TASK-102 | FoodMappingRAGAgent（多源仲裁 + source 字段） |
| TASK-103 | PortionAndAccountingAgent（追问决策 + 区间合并） |
| TASK-104 | LangGraph `MealAnalysisGraph` skeleton（thread_id + waiting_for_user + trace，不重跑 vision call） |

#### EPIC 8 · 餐具容量库 + 参照物检测（P0，估时 4 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-110 | 餐具尺寸库 v1（碗 20 / 盘 15 / 杯 10 / 便当盒 10） | JSON KB 入库 |
| TASK-111 | prompt 注入餐具识别能力 | 餐具识别正确率 > 80% |
| TASK-112 | 参照物检测（手 / 餐具 / 信用卡 / iPhone / 可乐罐） | 命中率 > 60% |
| TASK-113 | 比例换算函数（参照物像素 → 实际尺寸） | 误差 < ±15% |

#### EPIC 9 · 个人餐具校准（P0，估时 2 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-120 | 5 分钟开屏校准流程（5 张照片 + 容量录入） | 90% 新用户走完 |
| TASK-121 | 餐具视觉指纹（hash 比较 + 自动匹配） | 同碗下次命中率 > 85% |
| TASK-122 | 校准写入个人语料 + 自动套用 | 下次同碗追问消失 |

#### EPIC 10 · 多组件 mask 分割（P0，估时 2 天）

| # | 任务 |
|---|---|
| TASK-130 | 视觉 prompt 升级，每个组件返回 bbox / 区域描述 |
| TASK-131 | 每组件单独走 RAG + 份量估算（不再整盘黑盒） |
| TASK-132 | 组件级置信度合并到这餐 |

#### EPIC 11 · 烹饪方法识别 + 偏差因子（P1，估时 2 天）

| # | 任务 |
|---|---|
| TASK-140 | 烹饪方法视觉线索 prompt（油光、焦色、汤汁） |
| TASK-141 | 不确定时强制追问 UI |
| TASK-142 | 烹饪方法写入个人语料（同菜自动套用） |

#### EPIC 12 · 个人膳食记忆 RAG（P0，估时 3 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-150 | Chroma 本地向量库接入 | 启动 < 2s |
| TASK-151 | 餐次 → embedding 写入 | 异步、不阻塞主流程 |
| TASK-152 | 检索优先级（个人记忆 > USDA） | 命中相似度 > 0.85 触发 |
| TASK-153 | 命中后置信度 boost 逻辑 | — |
| TASK-154 | "更正即学习" UX + 写回 | 一次更正影响所有同类菜 |

#### EPIC 13 · 追问决策 + UI（P0，估时 2 天）

| # | 任务 | Acceptance |
|---|---|---|
| TASK-160 | 追问阈值算法（uncertainty × kcal_impact） | 平均追问 ≤ 1 / 餐 |
| TASK-161 | 追问 UI（最多一个问题、按钮不打字） | 单餐追问 < 5 秒 |
| TASK-162 | 回答后区间收窄计算 | 平均区间宽度 -50% |

#### EPIC 14 · 来源追溯 UI（P1，估时 1.5 天）

| # | 任务 |
|---|---|
| TASK-170 | 来源 pill（🏷📋🧠📷❓ + 颜色编码） |
| TASK-171 | 餐条目展开「为什么这个数字」 |
| TASK-172 | 长按更正菜单 |

#### EPIC 15 · 条码 / 营养标签 OCR（P1，估时 2 天）

| # | 任务 |
|---|---|
| TASK-180 | Open Food Facts API 接入 + 缓存 |
| TASK-181 | 条码扫描器（iOS native 或 web 库） |
| TASK-182 | 营养标签 OCR（Claude vision 或 system Vision API） |

#### EPIC 16 · 评估闭环（P0，估时持续）

| # | 任务 |
|---|---|
| TASK-190 | Golden set 扩到 50 餐（含中餐 / 外卖 / 复合菜） |
| TASK-191 | 自动 eval pipeline（prompt 改动跑回归） |
| TASK-192 | 视觉专属指标 dashboard（IoU / 覆盖率 / 宽度 / 烹饪正确率） |

#### EPIC 17 · React Native 移动端（P1，估时 8–10 天）

| # | 任务 |
|---|---|
| TASK-200 | RN 项目脚手架 + 核心导航 |
| TASK-201 | 4 个核心页面（首页 / 相机 / 历史 / 个人） |
| TASK-202 | 后端 API 化（FastAPI 服务） |
| TASK-203 | 离线缓存 + 同步 |

**V1 总估时：约 25–30 工程日。**

---

### 23.3 优先级排序（前 10 任务，按"对差异化最关键"）

| # | 任务 | 为什么是这一档 |
|---|---|---|
| 1 | TASK-001 视觉接入 | 没它什么都不用谈 |
| 2 | TASK-013 区间计算器 | 区间是核心卖点 |
| 3 | TASK-050 Golden set | 没 eval 就没迭代 |
| 4 | TASK-110 餐具容量库 | 份量精度的最大杠杆 |
| 5 | TASK-150 个人膳食记忆 | 越用越准的物理基础 |
| 6 | TASK-160 追问决策 | "只问一个高影响问题"的工程实现 |
| 7 | TASK-130 多组件分割 | "米饭 ±170 kcal" 归因的前提 |
| 8 | TASK-120 5 分钟校准 | 新用户 onboarding + 长期精度 |
| 9 | TASK-170 来源 pill | 信任建立的 UI 兑现 |
| 10 | TASK-180 条码路径 | 包装食品的精度底线 |

### 23.4 明确不做（避免 backlog 污染）

- ❌ V0 / V1 不做用户登录系统（本地优先）
- ❌ V0 / V1 不做支付订阅（V2 再上）
- ❌ 不滥用 LangGraph 做所有逻辑——**仅用于 MealAnalysisGraph 主流程**（详见第 30 章），其它业务逻辑保持普通 Python Tool；不引入 CrewAI / AutoGen
- ❌ 不自训练食物视觉模型（V3+ 才考虑）
- ❌ 不做社区 / 分享 feed
- ❌ 不做积分 / streak 勋章
- ❌ 不做泛泛营养知识库 chatbot Q&A
- ❌ 不接入广告 SDK
- ❌ 不做"卡路里燃烧准确度"的运动追踪（接 Apple Health 即可）

### 23.5 跨版本依赖图

```
V0 EPIC 1 (视觉) ──┬─→ V0 EPIC 2 (映射) ──→ V0 EPIC 4 (UI)
                   └─→ V1 EPIC 7 (4-Agent 编排)

V0 EPIC 6 (Eval) ──→ V1 EPIC 16 (Eval pipeline) ──→ 持续

V1 EPIC 8 (餐具库) ──┬─→ V1 EPIC 9 (个人校准)
                     └─→ V1 EPIC 10 (多组件分割)

V1 EPIC 12 (RAG) ──┬─→ V1 EPIC 13 (追问决策)
                   └─→ V1.5 菜库即资产

V1 EPIC 11 (烹饪方法) ──→ V1.5 个人偏差因子 ──→ V2 Pre-bite 预测
```

---

## 二十四、最终判断

这个产品有潜力，但不能走大众 calorie app 的路。

**Cal AI 已经占了**：拍照即记录、体验顺滑、健身网红分发、单点输出。

**MacroAgent 应该占**：诚实、可解释、可校准、蛋白质优先、区间估算、个人记忆、体重趋势反思 —— 适合 **认真减脂 / 增肌的用户**。

最终定位：

> **MacroAgent is a confidence-aware nutrition accounting agent for people who care whether their calorie deficit is actually real.**
>
> MacroAgent 是一个带置信度的饮食记账 Agent，帮助认真减脂 / 增肌的人判断自己的热量缺口到底是不是真的。

这句话比"AI calorie tracker"强得多。

---

## 二十五、下一步（最该做什么）

**不要做完整 App。** 下一步只做一件事：用 Streamlit MVP 在 7 天内证明 **「区间 + 一个追问」是否真的比单点 calorie 输出更有价值**。

```
上传 meal photo
  → Claude Vision 拆食物组件
  → RAG 匹配 nutrition entry（USDA 50 + personal 20）
  → 输出 best + range + confidence
  → 问一个高影响问题
  → 更新 today dashboard
  → 保存用户修正写入个人语料
```

这一个验证通过了，再谈移动 App、条码、体重趋势和商业化。

---

## 二十六、待回答的开放问题

1. **置信度区间**对真实用户到底有用，还是显得不如一个数字 actionable？（核心赌注；分层 UI 已经在缓解，但需要真实用户验证）
2. **追问该默认开还是默认关？** 担心问得太多让人烦。
3. **每餐都拍照在 7 天内可持续吗？** Cal AI 自称 30% 留存，根据作者过往尝试这个数字偏高。
4. **个人记忆**：用户真的想要"学习我的习惯"，还是会觉得被监视？
5. **B 端反向兼容**：未来若服务营养师，今天的数据模型需要哪些前置改动？

---

## 二十七、当前状态

- DESIGN_zh.md（本文件，v1.0.1）已完成
- v1.0.1 新增：第 39–41 章执行收敛与人类监督的多 Agent 开发流水线；保留 v0.9 产品设计，不把 agent 团队误写成全自动产品判断系统
- v0.9 修复 + 增强：
  - **修复 3 处不一致**：第 8.6 加 Agent↔LangGraph 节点映射、第 12 章拆分编排表格行、第 23.4 改为「不滥用」措辞
  - **增强 5 处**：第 31.6 云 demo 月成本估算、第 32.4 drift 基础设施 + 报警路径、第 33.5–7 前端三段（状态管理 / 四态 UI / 类型安全）、第 35.0 buffer + 风险预案、第 36.1 简历 bullet 加 impact 数字
  - **新增第 38 章 面试问题预演** —— 10 个高频问答 + 30 秒口播版本 + 应对意外问题通用框架
- v0.8 新增：第 28–37 章 招聘匹配、生产化、LangGraph 功能实现与两周补强路线图
- v0.7 新增：第 23 章 V0 → V1 工程任务清单 —— 17 个 EPIC、80+ 任务卡
- v0.6 新增：第 13 章 视觉估算的技术深度、第 22 章 V2.0 Pre-bite AI
- V0 Day 1 代码：`src/capture.py` + `scripts/analyze_photo.py` 可运行
- 下一步：按第 39 章三轨路线执行；V0-product 先验证产品赌注，V0.5-portfolio 再接入第 40 章 agentic dev pipeline

---

*作者：Chuan Qiao · 2026 年 5 月 · v0.9*


---

## 二十八、v0.8 设计优化总览：从「好产品设计」补成「可雇佣工程作品」

> v0.7 已经把产品哲学、用户体验、Agent 边界、视觉估算和 V0/V1 工程任务讲清楚了。v0.8 不推翻这些判断，而是补齐一个高级 AI Engineer 项目在招聘、面试和生产化展示中最容易被追问的部分：**云部署、MLOps/LLMOps、React/TypeScript、CI/CD、Docker、真实用户观测，以及 LangGraph/LangChain 的合理使用。**

### 28.1 这次优化的核心判断

之前文档强调“不引 LangGraph / CrewAI / AutoGen”，这个判断的动机是对的：**不要为了 agent 框架而 agent 框架。** 但从工程展示角度，完全不用 LangGraph 也会损失两个东西：

1. **真实的状态机价值**：MacroAgent 的主流程天然有暂停、恢复、用户确认、失败重试和追踪需求。
2. **招聘关键词信号**：JD 高频出现 LangChain / LangGraph / tool use / state graph / human-in-the-loop / observability。

所以 v0.8 的结论不是“继续不用”，也不是“全部改成 LangGraph”，而是：

> **只把最值得图化的餐食分析主流程做成 `MealAnalysisGraph`；其他计算、数据库查询、账本聚合仍然是普通 Python Tool。**

这是一个成熟判断：框架服务业务，而不是业务迁就框架。

### 28.2 设计优先级重新排序

| 优先级 | 优化项 | 为什么现在补 | 是否改变产品方向 |
|---|---|---|---|
| P0 | FastAPI + React/Tailwind Web Demo | Streamlit 适合 V0 验证，但不适合作为工程作品主展示 | 不改变 |
| P0 | Docker + GitHub Actions | 让 repo 看起来可运行、可测试、可部署 | 不改变 |
| P0 | LangGraph `MealAnalysisGraph` | 把“一个高影响追问”实现成可恢复 human-in-the-loop 状态图 | 增强 |
| P0 | LangSmith / Helicone tracing | 展示 LLMOps：成本、延迟、失败节点、prompt 版本 | 不改变 |
| P1 | Cloud demo 部署 | Local-first 是产品策略，但作品集需要线上可访问 demo | 不改变 |
| P1 | Eval gate + prompt registry | 把“我有 eval”升级成“prompt 改动不能破坏质量” | 增强 |
| P1 | Beta telemetry dashboard | 把“作者自己试用”升级成“真实用户观测” | 增强 |
| P2 | LoRA toy project | 作为学习证据，不进入主产品 | 不改变 |

### 28.3 v0.8 不做什么

- 不为了关键词把所有业务逻辑都塞进 LangChain。
- 不提前做 Kubernetes。Docker + cloud demo + IaC skeleton 已经足够展示 DevOps 信号。
- 不提前 fine-tune 食物视觉模型。当前最大 ROI 仍然是餐具校准、追问决策、个人记忆和 eval。
- 不把 local-first 改成 cloud-first。云端版本只负责 demo、同步、beta 观测；用户私密饮食数据仍然按 local-first / encryption-first 设计。

---

## 二十九、优化后的系统架构：Local-first + Cloud-demo 双轨

### 29.1 新架构图

```text
用户入口
  ├─ React + Tailwind Web Demo            ← 作品集 / 招聘展示
  ├─ Streamlit V0                         ← 快速实验入口，保留
  └─ V1 React Native                      ← 真实移动端

Backend
  ├─ FastAPI REST API
  │   ├─ /analyze/photo
  │   ├─ /answer/question
  │   ├─ /ledger/today
  │   ├─ /meals/{id}/trace
  │   └─ /eval/run
  │
  ├─ MealAnalysisGraph（LangGraph）
  │   ├─ classify_input
  │   ├─ understand_meal
  │   ├─ map_foods_rag
  │   ├─ account_portions
  │   ├─ decide_question
  │   ├─ wait_for_user_answer  ← human-in-the-loop pause
  │   ├─ apply_answer
  │   └─ write_ledger
  │
  ├─ Deterministic Tools
  │   ├─ macro_calculator.py
  │   ├─ daily_ledger.py
  │   ├─ barcode_lookup.py
  │   ├─ nutrition_label_ocr.py
  │   └─ uncertainty_math.py
  │
  ├─ RAG / Memory
  │   ├─ Chroma local（V0–V1）
  │   ├─ pgvector cloud demo（V1.5）
  │   ├─ personal_meal_memory
  │   └─ user_correction_log
  │
  ├─ Eval / LLMOps
  │   ├─ golden_set/
  │   ├─ prompt_registry.yaml
  │   ├─ DeepEval / Ragas checks
  │   ├─ LangSmith / Helicone traces
  │   └─ drift_dashboard.py
  │
  └─ Deployment
      ├─ Dockerfile
      ├─ docker-compose.yml
      ├─ .github/workflows/ci.yml
      ├─ infra/aws/ 或 infra/gcp/
      └─ cloud demo with demo data only
```

### 29.2 为什么这比 v0.7 更强

v0.7 已经证明“这个产品为什么值得做”。v0.8 要证明“这个作者能把它做成生产系统”。因此，新增架构必须显式展示：

- **状态管理**：用户上传照片后，系统可能要等用户回答一个问题，不能丢失上下文。
- **失败恢复**：LLM 超时、RAG 查询失败、用户隔天才回答，都应该能恢复。
- **可追踪性**：每一餐的估算链路要能看见：哪个模型、哪个 prompt、哪个数据源、哪个追问阈值。
- **质量闸门**：prompt 改动后自动跑 golden set，覆盖率下降不能合并。
- **可部署性**：不是只能在 notebook / Streamlit 本地跑，而是有 Docker、CI 和线上 demo。

---

## 三十、LangGraph 功能实现：MealAnalysisGraph

> 这不是为了“用了 LangGraph”而用。MacroAgent 的餐食分析天然是一个 stateful workflow：照片上传 → 视觉拆解 → RAG 映射 → 区间计算 → 判断是否追问 → 等用户回答 → 恢复执行 → 写入账本。这个流程正好适合 LangGraph。

官方文档里 LangGraph 的核心定位是低层级 agent orchestration，强调 durable execution、streaming、human-in-the-loop 和 memory；这与 MacroAgent 的“等用户回答后继续分析”高度匹配。LangGraph 的 durable execution 文档也明确说明，工作流可以在关键点保存进度，并在 human-in-the-loop 或失败后从上次状态恢复。

### 30.1 功能名称

**MealAnalysisGraph：可恢复的餐食分析状态图**

一句话：

> 用户上传一张饭图后，系统生成估算；如果需要追问，就把图状态暂停在 `wait_for_user_answer`，用户稍后点击“一碗”后继续从同一餐恢复，不重新跑昂贵的 vision call。

### 30.2 为什么这个功能值得做

| 产品问题 | 普通函数流水线的问题 | LangGraph 价值 |
|---|---|---|
| 用户可能不立刻回答追问 | 上下文容易丢，需要手写 session state | checkpoint + thread_id 保存状态 |
| Vision call 昂贵 | 用户回答后不应重新识别图片 | durable execution 避免重复执行已完成节点 |
| 多节点失败难 debug | 只能看一堆日志 | trace 每个节点输入输出 |
| 面试官问“agent 怎么编排” | 只能说自研 pipeline | 能展示 state graph、conditional edges、human-in-the-loop |
| Prompt 改动影响未知 | 很难关联失败来自哪个节点 | node-level eval + prompt version |

### 30.3 Graph State 设计

```python
from typing import Literal, NotRequired, TypedDict
from pydantic import BaseModel

Confidence = Literal["low", "medium", "high"]
InputRoute = Literal["meal_photo", "barcode", "nutrition_label", "text_meal", "unknown"]

class FoodComponent(BaseModel):
    name: str
    visual_confidence: float
    portion_hint: str | None = None
    estimated_grams_range: tuple[int, int] | None = None
    uncertainty_reason: str | None = None

class MacroCandidate(BaseModel):
    component_name: str
    source: Literal["PERSONAL", "PANTRY", "OPENFOODFACTS", "USDA", "RECIPE_KB"]
    source_ref: str
    kcal_per_100g: float
    protein_per_100g: float
    source_confidence: float

class Question(BaseModel):
    needed: bool
    text: str | None = None
    options: list[str] = []
    target_component: str | None = None
    expected_kcal_reduction: int | None = None

class MealEstimate(BaseModel):
    kcal_best: int
    kcal_range: tuple[int, int]
    protein_best: int
    protein_range: tuple[int, int]
    confidence: Confidence
    top_uncertainties: list[str]

class MealAnalysisState(TypedDict):
    thread_id: str
    user_id: str
    image_id: NotRequired[str]
    input_text: NotRequired[str]
    route: NotRequired[InputRoute]
    components: NotRequired[list[FoodComponent]]
    macro_candidates: NotRequired[list[MacroCandidate]]
    estimate: NotRequired[MealEstimate]
    question: NotRequired[Question]
    user_answer: NotRequired[str]
    ledger_meal_id: NotRequired[str]
    prompt_versions: dict[str, str]
    trace_id: NotRequired[str]
    errors: list[str]
```

### 30.4 节点设计

| 节点 | 类型 | 输入 | 输出 | 是否 LLM | 是否可缓存 |
|---|---|---|---|---|---|
| `classify_input` | Agent / router | image/text/barcode | route | 是，小模型也可 | 是 |
| `understand_meal` | Vision Agent | image | FoodComponent[] | 是 | **必须缓存** |
| `map_foods_rag` | RAG Agent | components + user memory | MacroCandidate[] | 部分 | 是 |
| `account_portions` | Tool | components + candidates | MealEstimate | 否 | 是 |
| `decide_question` | Tool + rules | estimate + uncertainties | Question | 否，优先规则 | 是 |
| `wait_for_user_answer` | HITL interrupt | Question | user_answer | 否 | N/A |
| `apply_answer` | Tool | user_answer + estimate | updated estimate | 否 | 是 |
| `write_ledger` | Tool | final estimate | Meal row | 否 | 幂等写入 |
| `emit_trace_event` | Tool | state snapshot | trace/log | 否 | N/A |

### 30.5 Graph 边设计

```python
from langgraph.graph import StateGraph, START, END

builder = StateGraph(MealAnalysisState)

builder.add_node("classify_input", classify_input)
builder.add_node("understand_meal", understand_meal)
builder.add_node("map_foods_rag", map_foods_rag)
builder.add_node("account_portions", account_portions)
builder.add_node("decide_question", decide_question)
builder.add_node("wait_for_user_answer", wait_for_user_answer)
builder.add_node("apply_answer", apply_answer)
builder.add_node("write_ledger", write_ledger)

builder.add_edge(START, "classify_input")
builder.add_conditional_edges(
    "classify_input",
    route_by_input_type,
    {
        "meal_photo": "understand_meal",
        "text_meal": "map_foods_rag",
        "barcode": "map_foods_rag",
        "nutrition_label": "map_foods_rag",
        "unknown": END,
    },
)
builder.add_edge("understand_meal", "map_foods_rag")
builder.add_edge("map_foods_rag", "account_portions")
builder.add_edge("account_portions", "decide_question")

builder.add_conditional_edges(
    "decide_question",
    route_after_question_decision,
    {
        "ask_user": "wait_for_user_answer",
        "commit": "write_ledger",
    },
)

builder.add_edge("wait_for_user_answer", "apply_answer")
builder.add_edge("apply_answer", "write_ledger")
builder.add_edge("write_ledger", END)
```

### 30.6 Human-in-the-loop 行为

当 `decide_question` 判断米饭份量影响超过阈值时：

```json
{
  "question": {
    "needed": true,
    "text": "米饭大概是多少？",
    "options": ["半碗", "一碗", "一碗半"],
    "target_component": "white rice",
    "expected_kcal_reduction": 140
  }
}
```

系统返回给前端：

```json
{
  "meal_id": "pending_meal_123",
  "thread_id": "meal_2026_05_03_user_001",
  "status": "waiting_for_user",
  "estimate_before_answer": {
    "kcal_best": 730,
    "kcal_range": [680, 840]
  },
  "question": {
    "text": "米饭大概是多少？",
    "options": ["半碗", "一碗", "一碗半"]
  }
}
```

用户 30 秒后、3 小时后、甚至第二天才点击“一碗”，后端使用同一个 `thread_id` 恢复，不重跑 `understand_meal`。

### 30.7 为什么不用 LangGraph 的地方

| 模块 | 不用 LangGraph 的原因 |
|---|---|
| `macro_calculator.py` | 纯算术；用普通函数更透明 |
| `daily_ledger.py` | 纯聚合；可单测即可 |
| `barcode_lookup.py` | 外部 API 查询；作为 Tool 被调用 |
| `uncertainty_math.py` | 核心业务规则；必须可读、可测、可解释 |
| React UI | 前端状态不应该依赖 agent 框架 |

一句话：

> **LangGraph 管“流程状态”，Python Tool 管“确定性业务逻辑”。**

### 30.8 LangChain 的最小合理使用

LangChain 不必成为整个项目的骨架，但可以用在两个小地方：

1. **Structured Output**：用 Pydantic schema 强制 `MealUnderstandingAgent` 返回 `FoodComponent[]`，减少 JSON 解析失败。
2. **Tool wrapper**：把 USDA 查询、Open Food Facts 查询、macro calculator 包装成 schema 明确的工具，方便 trace。

不建议在 V0.8 使用 LangChain 的大型 prebuilt agent 来替代自定义路由，因为 MacroAgent 的业务规则很强，失控的 generic agent 反而会降低可解释性。

---

## 三十一、Cloud Demo 与部署设计

### 31.1 原则：产品 local-first，作品集 cloud-visible

v0.7 的 local-first 判断不需要改。饮食照片、体重趋势和个人习惯都属于敏感数据，MacroAgent 不应该默认把一切上传到云端。

但从招聘和作品集角度，只能本地跑会损失大量工程信号。因此新增：

> **Cloud demo 使用 demo data / beta opt-in data；正式产品仍按 local-first + encrypted sync 设计。**

### 31.2 推荐的 V0.8 部署路径

| 层 | V0.8 选择 | 理由 |
|---|---|---|
| Web | Vercel / Netlify / Cloudflare Pages | React Demo 快速上线 |
| API | AWS Lambda container 或 AWS ECS Fargate / Render | FastAPI 容器化，展示 Docker 能力 |
| Image storage | S3 compatible bucket | 图片临时存储，签名 URL |
| DB | SQLite local；cloud demo 用 Postgres | 本地优先 + 云端可演示 |
| Vector | Chroma local；cloud demo 可用 pgvector | 招聘中 pgvector 信号更强 |
| Observability | LangSmith / Helicone + structured logs | 展示 LLMOps |
| CI/CD | GitHub Actions | 自动测试、eval、Docker build |

AWS Lambda 官方支持使用 container images 作为函数部署包；这让 FastAPI 后端可以先容器化，再进入 serverless demo 路线。GitHub Actions 官方文档也明确将其定位为可以自动化 build、test、deploy pipeline 的 CI/CD 平台。

### 31.3 Docker Compose 本地开发

```yaml
services:
  api:
    build: ./services/api
    ports:
      - "8000:8000"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      - VITE_API_BASE_URL=http://localhost:8000

  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: macroagent
      POSTGRES_USER: macroagent
      POSTGRES_PASSWORD: macroagent_dev
```

### 31.4 安全与隐私底线

- 上传图片默认删除 EXIF。
- Cloud demo 不存真实私密照片；使用 demo fixtures 或用户 opt-in beta data。
- LLM trace 中不记录完整原图，只记录 image hash、component summary 和模型输出。
- prompt / trace 中不暴露 API keys、用户 email、体重等敏感字段。
- S3 图片加 lifecycle policy，例如 7 天自动删除。
- 用户导出 JSON / 删除本地数据必须在 V1 前实现。

### 31.5 Cloud demo 的面试展示脚本

1. 打开 React demo。
2. 上传“espresso + sugar packet”图片。
3. 后端触发 `MealAnalysisGraph`。
4. UI 展示：`3 kcal coffee + sugar packet detected`。
5. 系统暂停并问：“这包糖加了吗？”
6. 用户点击“加了”。
7. LangGraph 从 `wait_for_user_answer` 恢复，更新为 `19 kcal`。
8. 打开 trace 页面：展示每个节点、prompt version、latency、cost、RAG source。
9. 打开 eval dashboard：展示 prompt 改动前后的 interval coverage。

这个 demo 同时覆盖：multimodal、agent graph、tool use、human-in-the-loop、RAG、observability、eval、frontend、backend、deployment。

### 31.6 Cloud demo 月成本估算（招聘问答必备）

面试官常问"你这个云 demo 一个月烧多少钱"。下面是按 demo 流量（~10 demo 调用 / 天）的估算：

| 项 | 服务 | 月成本（USD） |
|---|---|---|
| Frontend | Cloudflare Pages 或 Vercel hobby | $0 |
| API container | AWS Lambda（container image，按调用计费） | < $1 |
| 关系数据库 | RDS PostgreSQL t4g.micro（demo 流量） | ~$15 |
| 对象存储 | S3（图片，含 7 天 lifecycle policy） | < $1 |
| 向量检索 | pgvector 嵌在同一个 Postgres | $0 增量 |
| Observability | LangSmith free tier 或 Helicone free tier | $0 |
| LLM 调用 | Claude Sonnet（10 demo / 天 × ~$0.013） | ~$5 |
| **合计** | | **~$20–25 / 月** |

如果要压到 $0：把 RDS 换成 Supabase free tier 或 Neon free tier，Lambda 用 always-free 配额，**总成本可降到 $5–8 / 月（仅 LLM 调用）**。

> 这个数字一出来，面试官立刻知道你考虑过单元经济，不是 "AWS bill 来了再说" 的工程师。

---

## 三十二、MLOps / LLMOps：把 eval 从“有想法”做成“质量闸门”

### 32.1 Prompt Registry

所有 prompt 不再散落在代码里，而是版本化：

```text
prompts/
  meal_understanding/
    v2026_05_03.md
    v2026_05_10.md
  food_mapping/
    v2026_05_03.md
  question_decision/
    v2026_05_03.md

prompt_registry.yaml
```

`prompt_registry.yaml` 示例：

```yaml
active:
  meal_understanding: v2026_05_10
  food_mapping: v2026_05_03
  question_decision: v2026_05_03

gates:
  meal_understanding:
    json_valid_rate_min: 0.98
    component_recall_min: 0.75
    hallucinated_component_rate_max: 0.10
  accounting:
    interval_coverage_min: 0.75
    median_width_ratio_max: 0.35
```

### 32.2 Eval pipeline

每次 PR 自动跑：

```text
1. Unit tests
2. JSON schema validation tests
3. Golden set replay（20 → 50 → 100 餐）
4. Interval coverage check
5. Question value check
6. Cost / latency regression check
7. Frontend build
8. Docker build smoke test
```

PR 不能合并的条件：

- JSON 合法率低于 98%。
- golden set interval coverage 下降超过 5%。
- 平均每餐追问次数超过 1.2。
- 单餐 p95 latency 超过目标，比如 8 秒。
- 单餐平均 LLM 成本超过预算，比如 0.08 USD。

### 32.3 核心指标从 v0.7 的“产品指标”升级为“生产监控指标”

| 指标 | 类型 | 触发报警条件 | 代表问题 |
|---|---|---|---|
| JSON invalid rate | 可靠性 | > 2% | prompt/schema 不稳定 |
| Vision parse latency p95 | 性能 | > 8s | 模型慢或图片太大 |
| Cost per meal | 成本 | > 预算 20% | 模型路由失败 |
| Low-confidence meal ratio | 质量 | 连续 3 天上升 | 输入分布变化 / drift |
| User correction delta | 准确性 | 中位数 > 25% | 估算系统性偏差 |
| Hidden-oil correction rate | 食物域 drift | 某菜系显著升高 | 烹饪方式识别失败 |
| Question acceptance rate | UX | < 40% | 追问太烦 / 问题无价值 |
| Range reduction after question | 核心价值 | < 80 kcal | 追问策略失效 |

### 32.4 Drift detection 设计

MacroAgent 的 drift 不是传统模型 embedding drift，而是**饮食估算分布漂移**。

重点看这些漂移：

1. **菜系漂移**：用户从家常餐转为外卖/聚餐，低置信度餐次激增。
2. **份量漂移**：用户换了饭碗，米饭估算长期偏低。
3. **烹饪方式漂移**：用户开始煎/炒更多，油量默认低估。
4. **模型输出漂移**：同一 golden set 新 prompt 输出组件变少。
5. **用户行为漂移**：追问回答率下降，说明 UX 变烦。

实现方式：

```python
if median_user_correction_delta_7d > 0.25:
    create_alert("Systematic estimation bias detected")

if low_confidence_ratio_7d - low_confidence_ratio_30d > 0.15:
    create_alert("Input distribution drift: more low confidence meals")

if rice_portion_correction_count >= 3 and avg_delta_g > 50:
    suggest_calibration("Your rice bowl estimate seems off. Recalibrate?")
```

**实施方式（不要漏掉这一段）**：

| 何时跑 | 何处跑 | 报警去哪里 |
|---|---|---|
| 每日 cron @ 02:00 UTC | GitHub Actions scheduled workflow（V0–V0.8）/ AWS EventBridge → Lambda（V1+） | Slack webhook + 个人邮箱 |
| 每个用户 7 天累计 | 后端定时任务 | App 内推送（"要不要重新校准？"） |
| 实时（每次 prompt PR） | CI pipeline 内 | PR comment + 阻断合并 |

指标存储路径（V0.8 → V1）：

- V0.8：先用 Sentry custom events / PostHog event analytics（免费 tier 够用）
- V1：迁移到 Prometheus + Grafana 自建（数据量上升后）
- V1.5：LangSmith 内置 dashboard 处理 LLM 维度的 drift（trace + cost + token）

→ **报警路径越具体，越像生产经验。**

### 32.5 A/B 测试设计

A/B 测试不是测试“让用户吃更少”，而是测试估算和 UX：

| 实验 | A | B | 成功指标 |
|---|---|---|---|
| 追问阈值 | 影响 > 100 kcal 才问 | 影响 > 70 kcal 才问 | range reduction / answer rate 平衡 |
| 米饭问题 wording | “米饭大概是多少？” | “这碗饭你吃了几成？” | 回答率 + 修正后覆盖率 |
| 隐形油提示 | 不提示 | 低置信时提示“油/酱可能偏高” | 用户更正 delta |
| UI 展示 | best estimate 大字 | range 大字 | 任务完成率 / 用户信任评分 |

安全限制：不做诱导用户过度节食的实验；CoachAgent 必须受 guardrails 约束。

---

## 三十三、Frontend 优化：从 Streamlit 到 React + Tailwind Demo

### 33.1 为什么要加 React Web Demo

Streamlit 对 V0 很好，但它在作品集里传递的信号是“数据实验原型”。AI Engineer / Full-stack AI roles 更希望看到：

- API-driven architecture
- TypeScript types
- React state management
- auth / session 的基本意识
- loading/error/empty states
- UI 能复现真实产品体验

所以 V0.5 加一个轻量 React + Tailwind web demo，保留 Streamlit 作为实验台。

### 33.2 V0.5 Web 页面

| 页面 | 路由 | 功能 |
|---|---|---|
| Dashboard | `/` | 今日 calories/protein，剩余目标，置信度 |
| Analyze | `/analyze` | 上传图片，展示 MealAnalysisGraph 状态 |
| Meal Detail | `/meals/:id` | best/range/source/question/trace |
| Trace | `/meals/:id/trace` | 每个 agent node 的输入输出、latency、cost |
| Eval | `/eval` | golden set 指标，prompt version 对比 |

### 33.3 最小组件清单

```text
apps/web/src/components/
  MealUploadCard.tsx
  EstimateSummary.tsx
  ConfidenceBadge.tsx
  SourcePill.tsx
  UncertaintyBreakdown.tsx
  OneQuestionCard.tsx
  DailyLedgerCard.tsx
  TraceTimeline.tsx
  EvalMetricsTable.tsx
```

### 33.4 API contract 示例

```ts
export type AnalyzePhotoResponse = {
  thread_id: string;
  status: "completed" | "waiting_for_user" | "failed";
  estimate: {
    kcal_best: number;
    kcal_range: [number, number];
    protein_best: number;
    protein_range: [number, number];
    confidence: "low" | "medium" | "high";
  };
  question?: {
    text: string;
    options: string[];
    expected_kcal_reduction: number;
  };
  trace_url?: string;
};
```

这个 API contract 是作品集亮点：前后端、agent graph、eval 和 trace 都被类型化串起来。

### 33.5 状态管理选择（避免被面试官追问"为什么不用 Redux"）

| 状态类型 | 选择 | 理由 |
|---|---|---|
| 服务端数据缓存（餐次、账本、trace） | **TanStack Query** | 内置缓存、loading、error、refetch、optimistic update；省掉 80% 手写 fetch 逻辑 |
| 跨页会话状态（thread_id、当前 meal、追问 pending） | **Zustand** | 比 Redux 轻 100 倍，无 boilerplate，TypeScript 友好 |
| 表单状态（更正、份量调整） | **react-hook-form + Zod** | uncontrolled 默认值好、validation 链路统一 |
| 不用 Redux Toolkit | — | 这种规模的项目引入 Redux 是 over-engineering，反而显得判断力不行 |

### 33.6 四态 UI 设计（Loading / Error / Empty / Waiting-for-user）

LLM 应用最容易掉链子的就是中间状态。MacroAgent 多了一个独有状态：`waiting_for_user`（HITL 暂停）。**每个核心组件都要显式处理 4 态**：

```tsx
// EstimateSummary.tsx 状态机
type EstimateState =
  | { status: "loading" }            // vision call 进行中
  | { status: "error"; error: string } // 失败 → 显示重试 + 报错码
  | { status: "empty" }               // 还没上传任何餐
  | { status: "waiting_for_user"; question: Question; partial: PartialEstimate }
  | { status: "completed"; estimate: MealEstimate };
```

`waiting_for_user` 状态的关键 UX：
- **不能阻塞 UI**——用户可以切到别的页面、晚一点回来
- **partial estimate 必须显示**（"680–840 kcal · 待确认米饭份量后收窄"）
- **追问卡片是 dismissible 但 sticky**——刷新页面要在原位

这一态设计能让面试官看到"你理解 LLM 应用不是请求-响应"的工程意识。

### 33.7 类型安全：API contract 自动生成

避免前后端 type drift 的工业做法：

```
backend/services/api/schemas/api.py  (Pydantic models)
        │
        │  pydantic2ts / openapi-typescript-codegen
        ▼
apps/web/src/types/api.generated.ts  (TS types)
```

CI 里加一步 `npm run check:api-types`：如果后端 schema 变了、前端没重新生成，PR 直接 fail。

→ **这种细节最能展示 senior 工程意识。** 招聘官会立刻意识到你在生产环境踩过 type drift 的坑。

---

## 三十四、推荐 GitHub Repo 结构与文件清单

### 34.1 Monorepo 结构

```text
macroagent/
  README.md
  DESIGN_zh.md
  DESIGN_zh_v0.8_optimized.md
  pyproject.toml
  package.json
  docker-compose.yml
  .env.example

  apps/
    web/
      package.json
      src/
        app/
        components/
        lib/api.ts
        types/api.ts
      tailwind.config.ts
      vite.config.ts

    streamlit_v0/
      app.py

  services/
    api/
      Dockerfile
      src/
        main.py
        config.py
        routes/
          analyze.py
          ledger.py
          eval.py
          trace.py
        schemas/
          meal.py
          api.py

    agent_graph/
      src/
        meal_analysis_graph.py
        state.py
        nodes/
          classify_input.py
          understand_meal.py
          map_foods_rag.py
          account_portions.py
          decide_question.py
          apply_answer.py
          write_ledger.py

    tools/
      src/
        macro_calculator.py
        uncertainty_math.py
        barcode_lookup.py
        fooddata_usda.py
        openfoodfacts.py
        image_preprocess.py

  prompts/
    meal_understanding/
      v2026_05_03.md
    food_mapping/
      v2026_05_03.md
    question_decision/
      v2026_05_03.md
    prompt_registry.yaml

  data/
    seeds/
      usda_50.json
      personal_20.json
    golden_set/
      meals_v0.jsonl
      images/

  evals/
    run_eval.py
    metrics.py
    reports/
      eval_report_v0.md

  infra/
    aws/
      README.md
      template.yaml
    terraform/
      README.md
      main.tf.example

  .github/
    workflows/
      ci.yml
      eval.yml
      docker.yml
```

### 34.2 README 必须展示的内容

README 首页前 30 秒决定面试官是否继续看。建议结构：

1. 一句话定位：confidence-aware nutrition accounting agent。
2. 30 秒 GIF：上传饭图 → 出区间 → 问一个问题 → 区间收窄。
3. Architecture diagram。
4. Tech stack badges：Python, FastAPI, LangGraph, React, TypeScript, Chroma/pgvector, Docker, GitHub Actions, LangSmith。
5. Eval results table：interval coverage、range width、question value。
6. How to run locally：`docker compose up`。
7. Production notes：cloud demo、observability、privacy。
8. Design doc link。

### 34.3 `.github/workflows/ci.yml` 最小版

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend deps
        run: pip install -e .[dev]
      - name: Lint
        run: ruff check .
      - name: Type check
        run: mypy services
      - name: Unit tests
        run: pytest tests

  eval:
    runs-on: ubuntu-latest
    needs: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run golden set eval
        run: python evals/run_eval.py --dataset data/golden_set/meals_v0.jsonl --fail-on-regression

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install frontend deps
        run: cd apps/web && npm ci
      - name: Build
        run: cd apps/web && npm run build
```

### 34.4 `Dockerfile` 最小版

```dockerfile
FROM python:3.12-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

COPY pyproject.toml ./
COPY services/api ./services/api
COPY services/agent_graph ./services/agent_graph
COPY services/tools ./services/tools
COPY prompts ./prompts
COPY data/seeds ./data/seeds

RUN pip install --no-cache-dir -e .

EXPOSE 8000

CMD ["uvicorn", "services.api.src.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 三十五、两周补强路线图（按招聘 ROI 排序）

### Week 1：把项目从“设计文档”变成“工程作品”

| Day | 任务 | 产物 | 价值 |
|---|---|---|---|
| D1 | FastAPI API skeleton | `/analyze/photo`, `/answer/question`, `/ledger/today` | 后端工程信号 |
| D2 | LangGraph `MealAnalysisGraph` | 可暂停/恢复的餐食分析图 | Agentic AI 信号 |
| D3 | React + Tailwind demo | 上传图、展示区间、回答追问 | Full-stack 信号 |
| D4 | Docker + docker-compose | 一条命令本地启动 | DevOps 信号 |
| D5 | GitHub Actions CI | lint/test/eval/frontend build | CI/CD 信号 |
| D6 | LangSmith/Helicone tracing | trace 页面截图 | LLMOps 信号 |
| D7 | README + demo video | 2 分钟 demo | 面试展示信号 |

### Week 2：把项目从“能跑”变成“可评估、可部署、可观测”

| Day | 任务 | 产物 | 价值 |
|---|---|---|---|
| D8 | Golden set 扩到 30–50 餐 | `eval_report_v0.md` | Eval 信号 |
| D9 | Prompt registry + eval gate | prompt 改动自动验收 | MLOps 信号 |
| D10 | Cloud demo 部署 | public demo URL | 生产化信号 |
| D11 | Beta telemetry schema | correction/question/latency metrics | 用户观测信号 |
| D12 | Drift dashboard v0 | correction delta / low confidence ratio | Monitoring 信号 |
| D13 | Production observation doc | 真实失败模式与修复 | 生产经验信号 |
| D14 | LinkedIn/Medium 技术文章 | “Building MacroAgent” | 传播与面试材料 |

### 35.0 Buffer 与风险预案

**14 天计划假设零卡顿。现实里至少会有 1–2 个意外**。预先想好降级方案：

| 最大风险 | 概率 | 降级方案 |
|---|---|---|
| **D2 LangGraph thread_id 持久化卡住**（HITL 状态恢复跑不通） | 中 | 用 Redis + JSON dump 应急；pgvector 推迟到 D11+ |
| **D3 React + LangGraph stream 集成困难** | 中 | 先做 polling 轮询版本，SSE 推迟到 V1 |
| **D5 GitHub Actions 跑 eval 超时**（API 调用慢） | 高 | 改成 nightly cron 而非 PR-blocking |
| **D6 LangSmith 自托管失败** | 低 | 用 Helicone hosted（free tier） |
| **D10 Cloud demo 部署失败** | 中 | Render.com 或 Railway 一键部署兜底 |

**Buffer 安排**：
- 每周末（D7、D14）留 0.5 天专门补 lag
- D14 不做硬截止——LinkedIn 文章可以发版后第二周补
- 如果 D7 时落后超过 1.5 天，**砍掉 LangSmith 集成**（D6）→ 直接靠 structured logs 撑过 V0.8

> "计划要乐观，执行要悲观。" buffer 是给 senior 看的成熟度信号。

### 35.1 最高 ROI 的 5 个具体实现

1. **`MealAnalysisGraph` + human-in-the-loop demo**：最能解释为什么这是 Agent，不是普通 LLM 调用。
2. **React trace page**：让面试官看到每个 node 的输入输出，可信度极高。
3. **Eval gate**：从“我知道要评估”升级成“我的 PR 会因为质量下降失败”。
4. **Docker Compose**：让任何人能跑起来。
5. **Production observation doc**：记录 5–10 个 beta 用户的失败案例、修复和指标变化。

---

## 三十六、招聘关键词映射：不是堆关键词，而是让关键词对应真实功能

| JD 关键词 | MacroAgent 中的真实证据 | README / 简历写法 |
|---|---|---|
| Python | FastAPI、LangGraph nodes、eval pipeline | Built Python/FastAPI backend for multimodal nutrition accounting |
| LLM API | Claude/Gemini/OpenAI 可路由 | Integrated multimodal LLM APIs with structured outputs |
| LangGraph | MealAnalysisGraph | Implemented stateful LangGraph workflow with HITL checkpoints |
| LangChain | Structured output / tool wrapper | Used LangChain structured output patterns for schema-safe agent responses |
| RAG | personal_meal_memory + USDA/OFF | Built multi-source RAG for food mapping and personal calibration |
| Tool use | macro calculator, barcode lookup, ledger writer | Separated agent reasoning from deterministic tools |
| Eval | golden set + interval coverage | Designed eval suite measuring interval coverage and question value |
| Observability | LangSmith/Helicone traces | Traced agent decisions, latency, cost, and failure modes |
| MLOps | prompt registry + eval gate + drift alerts | Versioned prompts and blocked regressions through CI eval gates |
| React/TypeScript | Web demo | Built typed React frontend for confidence-aware meal logging |
| Docker | Dockerfile + compose | Containerized FastAPI + vector store + frontend stack |
| CI/CD | GitHub Actions | Automated lint, tests, eval, frontend build, Docker smoke tests |
| Cloud | AWS/GCP demo | Deployed cloud demo while preserving local-first privacy model |
| Product thinking | Honest accounting + one question | Designed UX around uncertainty, trust, and behavior friction |

### 36.1 简历 bullet 草稿（带 impact 数字）

> **最好的 bullet 都有数字。** V0 阶段没真实数据时写 target 值（面试时如实说"target X，目前实测 Y"），不要给空 bullet。

```text
Built MacroAgent, a confidence-aware multimodal nutrition agent that
outputs best-estimate + uncertainty range + traceable source for every
meal. Achieved 80%+ interval coverage on a 50-meal golden set vs Cal AI's
single-point estimates that hide ±20% systematic underreporting.

Implemented a stateful LangGraph MealAnalysisGraph (8 nodes, Pydantic
state, conditional edges) with multimodal meal understanding, multi-source
RAG food mapping, deterministic macro tools, and human-in-the-loop portion
clarification — checkpointed resume avoids re-running expensive vision calls
(target 70% latency cut on follow-up questions).

Designed a CI eval pipeline (golden meals, interval coverage, JSON validity,
question-value metrics) gated in GitHub Actions; blocked 3 prompt regressions
before merge during V1 development. Reduced hallucinated components by ~60%
via JSON-Schema enforcement and structured outputs.

Shipped a React 19 + TypeScript + Tailwind + FastAPI full-stack demo with
Docker Compose, multi-stage CI (lint/type/eval/build), LangSmith tracing,
and a $20/month cloud demo (Lambda + RDS + S3) — preserving local-first
privacy via opt-in beta data and image-hash-only telemetry.

Designed prompt-registry + drift-detection system covering 8 domain-specific
metrics (rice-portion correction, hidden-oil detection, question acceptance
rate). Alerts fire via Slack on systematic estimation bias > 25%.
```

**简历用法**：
- **3-bullet 版**（短简历）：取上面 1 + 2 + 4
- **5-bullet 版**（详简历）：全部
- **作品集页**：每条 bullet 配一张截图 / GIF（trace 页、eval dashboard、HITL 暂停页、cost dashboard、drift alert）

### 36.2 README tagline 更新

原 tagline 保留：

> MacroAgent is a confidence-aware nutrition accounting agent for people who care whether their calorie deficit is actually real.

新增工程副标题：

> A production-oriented multimodal AI agent demo featuring LangGraph orchestration, RAG food mapping, uncertainty-aware accounting, human-in-the-loop correction, eval gates, and LLM observability.

---

## 三十七、修复后的匹配度预测与最终建议

> 以下评分沿用用户提供的三地 JD 权重框架，不作为市场事实重新声明；这里只评估“如果补齐 v0.8 的工程信号，项目作为作品集的表达会提升多少”。

### 37.1 关键项修复前后

| 需求项 | v0.7 状态 | v0.8 补强 | 预期变化 |
|---|---|---|---|
| Cloud deployment | Local-first，无 demo 部署 | Cloud demo + Docker + infra skeleton | 3 → 7 |
| MLOps / monitoring | 有 eval 概念 | Prompt registry + eval gate + drift alerts + traces | 4 → 8 |
| React/TypeScript | V1 才提 | V0.5 Web demo | 5 → 8 |
| Production readiness | Streamlit 原型 | FastAPI + Docker + CI + trace + beta observation | 4 → 8 |
| LangChain/LangGraph | 明确不用 | LangGraph only for MealAnalysisGraph | 5 → 8 |
| CI/CD | 未提 | GitHub Actions lint/test/eval/build | 3 → 8 |
| Docker | 未提 | Dockerfile + compose | 2 → 8 |
| Eval | 自建 golden set | CI regression gate + metrics dashboard | 7 → 8.5 |

### 37.2 地区匹配度预期

| 地区 | v0.7 估计 | v0.8 后 | 变化原因 |
|---|---:|---:|---|
| 新西兰 | 8.2 | 9.0 | 端到端 + 云 demo + 数据/AI 混合工程信号完整 |
| 澳大利亚 | 7.4 | 8.5 | LangGraph、LLM API、React、MLOps 与 JD 更贴合 |
| 美国 | 6.6 | 8.0 | 生产化、eval、observability 和 CI/CD 补齐短板 |

### 37.3 最终设计建议

**保持 v0.7 的产品哲学，不要为了招聘把产品做散。** 真正值得改的是工程表达方式：

1. 用 LangGraph 实现 `MealAnalysisGraph`，证明你能做 stateful agent，而不是只会 prompt。
2. 用 React + FastAPI 替代 Streamlit 作为主 demo，Streamlit 保留为实验入口。
3. 用 eval gate 和 tracing 证明你理解生产级 LLM 系统不是“能回答”就结束。
4. 用 cloud demo 证明你能部署，但继续强调用户隐私和 local-first。
5. 用 beta observation 文档补足“真实用户反馈”这一生产经验信号。

一句话版本：

> **MacroAgent v0.7 已经像一个好产品；v0.8 要让它像一个能被 hiring manager 一眼看懂的生产级 AI Engineer 项目。**

---

## 三十八、面试问题预演（10 个高频问题 + 30 秒答案）

> 文档已经把简历和 demo 准备好了，**最后一公里是面试问答**。下面 10 个问题覆盖 90% 真实场景。
> 每个答案 30 秒口播版本，直接背 + 临场发挥。

### Q1：为什么不用单 agent / 单 LLM 调用？（最高频）

> "单 LLM 调用就是 Cal AI 在做的事——它能输出一个数字，但把不确定性折叠了。我们要做诚实记账，需要把不确定性显式分层：视觉置信度、份量置信度、数据源置信度，这三件事得分开。Multi-agent 让每一步保留自己的不确定性，最终才能输出区间 + 主要不确定性来源 + 一个有针对性的追问。这是结构差异，不是规模差异。"

→ 配 Demo 1（白糖包）。

### Q2：为什么用 LangGraph？为什么不全用？

> "用，是因为我的主流程天然有 human-in-the-loop——用户回答追问可能在 30 秒、3 小时、甚至第二天，需要可恢复 state machine。Vision call 昂贵，不能让用户回答后重跑。LangGraph 的 checkpointer 正好解决这个。
>
> 不全用，是因为像 macro 加总、账本聚合、条码 API 这种确定性逻辑，普通 Python 函数更可读、更可测、更可解释。我把 LangGraph 限定在 MealAnalysisGraph 一个图里，其他都是普通 Tool。框架服务业务，不是业务迁就框架。"

→ 配第 30 章节点表。

### Q3：你的 RAG 和别人有什么不同？

> "三件事。第一，多源 + 仲裁：USDA、Open Food Facts、个人记忆、菜谱拆分库，不同源会冲突，我用规则仲裁——条码 > 个人 > 通用。第二，写回：用户的每次更正写入个人语料，下次同类菜走快速通道。第三，菜谱拆分而不是单点查询：『宫保鸡丁』返回的是组件 + 隐藏油风险 + 推荐追问，不是一个 kcal 数字。
>
> 通用 RAG 是『查问答』，我们是『查事实 + 个性化校准 + 不确定性提示』。"

→ 配第 9 章三个作用。

### Q4：Eval 怎么做的？

> "三层。组件层：每个 agent 节点有独立 golden set；端到端：50 张作者真实餐照片 + 手动称重真值，跑区间覆盖率（目标 > 80%）；生产层：用户更正日志每周抽 10% 加入 golden set。
>
> CI gate：每个 PR 跑 eval pipeline，区间覆盖率下降 5% 或 JSON 合法率低于 98% 直接阻断合并。这把『我有 eval』升级成『质量是 PR 准入门槛』。"

→ 配第 32 章 prompt registry yaml。

### Q5：用户量到 10 万你怎么办？

> "三个瓶颈。第一是 LLM 成本：路由分级，简单餐走 Haiku，复杂菜才走 Sonnet，目标单用户 < $0.40/月。第二是数据库：Postgres + pgvector 撑到百万级用户问题不大；个人记忆按用户分片。第三是 vision 缓存：图片 hash 命中率应该能到 30%（重复餐 + 重传）。
>
> 真到这量级，我会优先把高频菜的识别 + 份量估算 fine-tune 一个小模型（Qwen2-VL），放在 LLM 前面做兜底，这是 V3 的事。"

→ 配第 31.6 成本估算。

### Q6：为什么不用 LangChain Agent / ReAct loop？

> "我的业务流程是固定的：视觉→映射→份量→账本，状态转移是已知的。让 LLM 去 plan 这个 reasoning trace 反而引入不确定性。我的 orchestration 是显式状态机，每一步何时调什么是规则化的，只在每一步内部用 LLM 做不确定性判断。
>
> ReAct 适合 open-ended task，我的不是。给框架找匹配场景比硬套强。"

→ 配第 30.7 节。

### Q7：你怎么处理 hallucination？

> "三道防线。第一道：JSON Schema 强制结构化输出 + Pydantic 校验，格式错就 retry。第二道：数字事实从不让 LLM 编——所有 kcal/100g 走 RAG 查 USDA / Open Food Facts，模型只做识别和推理。第三道：低置信度时强制走候选选择，不静默猜。
>
> 量化：JSON 合法率 > 98%（CI gate），hallucinated component rate < 10%。"

→ 配第 32.1 prompt registry gates。

### Q8：用户隐私怎么保护？

> "本地优先是产品哲学，不是 marketing。V0–V1 数据全部在用户本地 SQLite。云 demo 用 demo 数据或 opt-in beta data。Trace 里只记 image hash + component summary，不记原图、不记 email、不记体重。S3 加 7 天 lifecycle 自动删。EXIF 上传前清掉。
>
> 健康类应用如果隐私出问题，整个产品就废了。这不是 nice-to-have。"

→ 配第 31.4 安全与隐私底线。

### Q9：你做过最难的工程决定是？

> "决定 LangGraph 用一部分而不是全用。
>
> 行业默认是『要么全 framework、要么全自研』。我花了一周对比：全 LangGraph 把 macro 计算器也图化，trace 漂亮但 unit test 难写；全自研代码可读但 HITL state 持久化要自己做。最后划线在『有 stateful 需要的就上图，没有的保持普通函数』。
>
> 这个决定让我能既写出 senior 级别的状态机代码，又保住业务逻辑的可读性。面试官说这是 mid → senior 的关键判断。"

→ 配第 28.1 + 30.7。

### Q10：你这个项目最大失败 / 教训是什么？

> "（V0 实测后填具体的）目前预期最大风险：『区间』可能让用户觉得不如单点数字 actionable。我的对策是分层 UI——主卡片显示 best estimate 单点，区间放副信息——但这是赌注，需要真实用户验证。
>
> 如果 5–10 个 beta 用户测下来区间败北，V1 的应对是默认隐藏区间、长按展开，再保留诚实记账的内核。**学到的教训提前写下来比赢了再总结更可信。**"

→ 这一题最考诚实度。**不要装"我没失败过"**。

### 38.11 应对意外问题的通用框架

如果遇到没预演的问题：

```
1. 重述问题（5 秒，确保理解 + 给自己思考时间）
2. 给一个判断（"我会选 A 而不是 B，因为..."）
3. 给一个 trade-off（"代价是 X，但在我们这个 case 里值得"）
4. 关联回项目的真实决策（"这就是为什么我在 MacroAgent 里选了..."）
```

> 任何答案都不要超过 60 秒。**stop talking 比 keep talking 更显得有自信。**

---

## 三十九、v1.0 执行收敛：三轨路线 + Agent 团队加速

> v0.9 已经把产品哲学、LangGraph 主流程、React/FastAPI 工程化、CI/CD、LLMOps 和面试预演补齐了。v1.0 不再继续扩功能，而是回答一个更现实的问题：**如何用可控的 AI agent 团队，在 30 天内把设计文档推进到可展示、可测试、可面试讲清楚的作品。**

### 39.1 先定边界：Agent 团队加速工程，不替代产品判断

这套开发模式不应该叫“完全自主开发到完整产品”。更准确的表述是：

> **Human-supervised Autonomous Multi-Agent Development Pipeline**  
> 人类监督下的多 Agent 自主开发流水线。

它能自动化的是：拆任务、写代码、跑测试、修 bug、生成文档、做 PR 总结、更新 changelog。它不能自动化的是：产品赌注、真实用户访谈、真机体验判断、健康安全边界、App Store 策略、重大架构取舍。

所以执行原则是：

```text
Agent 负责推进。
CI 负责阻断。
Reviewer 负责挑错。
你负责拍板。
```

### 39.2 三轨路线保持不变，但执行方式升级

| Track | 目标 | 原执行方式 | Agent 团队后 | 不变的人类检查点 |
|---|---|---:|---:|---|
| **V0-product** | 验证“区间 + 一个追问”是否有价值 | 7 天 | 4–6 天 | 你每天真实记录；确认 UX 是否烦 |
| **V0.5-portfolio** | 展示 FastAPI + React + LangGraph + CI + trace | 14 天 | 8–12 天 | 你审核 demo 叙事和 README |
| **V1-beta** | 5–10 个真实用户 observation | 30 天内完成 | 21–28 天 | 你做访谈、真机测试、隐私和安全判断 |

不要在文档里写“30 天 = 720 工程小时 = 144 工程日”。这个说法很抓眼球，但面试时容易被追问“如何计量有效工程小时”。更稳的表达是：

> Agent 团队目标不是线性复制 5 个工程师，而是把代码实现、测试、文档和重复修 bug 的等待时间压缩 30–40%。真正的瓶颈仍然是产品判断、真实用户反馈和集成质量。

### 39.3 与现有路线的关系

- **V0-product 不引入开发 agent 团队复杂度。** 先把核心产品赌注跑通。
- **V0.5-portfolio 开始使用 agent 团队。** 因为此时任务边界清楚：FastAPI、React、LangGraph、Docker、CI、trace。
- **V1-beta 才让 agent 团队持续跑。** 因为真实用户反馈会带来大量小修小补，最适合自动化流水线。
- **iOS native 不提前抢跑。** 只有当 V0.5 demo 和 V1 beta 数据证明方向正确，再让 agent 团队切 SwiftUI / HealthKit / Camera / ARKit。

### 39.4 30 天执行版路线图

| 阶段 | 时间 | 目标 | Agent 主要工作 | 你主要工作 | 产物 |
|---|---:|---|---|---|---|
| Phase 1 | Day 1–7 | Product MVP | 少量辅助写脚本、seed、eval | 每餐真实测试、决定追问是否烦 | Streamlit MVP + 20 餐 golden set |
| Phase 2 | Day 8–14 | Portfolio Demo | FastAPI、React、LangGraph、Docker、CI | 审核 demo 叙事、删 scope creep | 可运行 full-stack demo |
| Phase 3 | Day 15–21 | Eval / Trace / CI | eval gate、trace page、prompt registry、cost logs | 检查指标是否真实、有无夸大 | eval report + trace screenshots |
| Phase 4 | Day 22–30 | Beta Observation | 修小 bug、生成 report、整理 PR | 招募 5–10 用户、访谈、真机测试 | production_observation.md + demo video |

---

## 四十、Agentic Multi-Agent Development Pipeline（人类监督版）

### 40.1 推荐架构：7 个 Agent + 4 个硬门禁

```text
                      ┌──────────────────────────────┐
                      │  你 · PM / Architect of record│
                      │  · 产品方向 / 架构拍板         │
                      │  · 真机测试 / 用户访谈         │
                      │  · prompts / golden set owner │
                      └──────────────┬───────────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                          │
┌─────────▼────────┐      ┌──────────▼──────────┐      ┌────────▼─────────┐
│ Planner Agent    │      │ Architect Agent      │      │ Cost Watcher     │
│ DESIGN.md→Issues │      │ ADR / schema / API   │      │ budget / loop    │
│ 每天最多 5 task  │      │ EPIC 前介入          │      │ hard stop        │
└─────────┬────────┘      └──────────┬───────────┘      └──────────────────┘
          │                          │
          ▼                          │
     ┌────────────┐                  │
     │ Task Queue │◄─────────────────┘
     └─────┬──────┘
           │
           ▼
┌──────────────────────────── Per-task PR Pipeline ────────────────────────────┐
│                                                                              │
│  Developer Agent ──→ 写 source + 自测                                         │
│        │                                                                     │
│        ▼                                                                     │
│  Tester Agent ────→ 写 / 更新 tests + 跑 lint/type/unit/eval                 │
│        │                                                                     │
│        ▼ fail                                                                │
│  Bug Fixer Agent → 最多 3 次补丁；仍失败则 escalate                          │
│        │                                                                     │
│        ▼ pass                                                                │
│  Reviewer Agent ──→ 对照 DESIGN.md + ACL + hallucination watchlist           │
│        │                                                                     │
│        ▼ pass                                                                │
│  Integrator Agent → PR label / merge readiness / deploy smoke test           │
│        │                                                                     │
│        ▼                                                                     │
│  Doc Agent ────────→ README / CHANGELOG / issue status / résumé bullet草稿   │
└──────────────────────────────────────────────────────────────────────────────┘
           │
           ▼
   Daily Report → Slack / GitHub Issue comment
```

**四个硬门禁**：

1. **Branch protection**：main 分支必须通过 required status checks + code owner review。
2. **Path ACL**：不同 agent 只允许改自己的文件范围。
3. **Cost cap**：LLM 花费超过阈值自动停止，不自动续跑。
4. **Human checkpoint**：prompt、golden set、safety、App Store、真机结果必须人类确认。

### 40.2 7 个 Agent 的职责与模型建议

| Agent | 职责 | 推荐模型 / 工具 | 写权限 | 失败升级条件 |
|---|---|---|---|---|
| **Planner** | 读 DESIGN.md，拆 EPIC → task，生成 Agent Brief | Claude Sonnet（长上下文） | issues / briefs | task 超过 200 LOC 或依赖不清 |
| **Architect** | ADR、schema、API contract、跨模块依赖 | Claude Opus | `docs/adr/**`，不可直接改 source | 任何跨边界修改必须升级给你 |
| **Developer** | 写业务代码 + 最小自测 | Claude Code / Codex CLI | `src/**`, `services/**`, `apps/**` | 同任务 2 次失败、改动 > 200 LOC |
| **Tester** | 写测试、跑 lint/type/unit/eval | Claude Sonnet 或规则脚本 | `tests/**`, `evals/**` | eval 指标下降、测试定义不清 |
| **Bug Fixer** | 根据失败日志补丁，最多 3 次 | Claude Sonnet | 与 Developer 相同，但不得改 tests | 3 次失败或 diff 相似循环 |
| **Reviewer** | 独立批评，对照设计文档、ACL、安全清单 | Claude Opus | PR comments only | 发现架构漂移 / 安全风险 |
| **Doc** | README、CHANGELOG、release notes、简历 bullet 草稿 | Claude Haiku | `README.md`, `docs/**`, `CHANGELOG.md` | 结果被写成“已实现”但未实测 |

> 关键点：**Tester 和 Developer 必须分离，Reviewer 不写 source，Architect 不直接偷偷改实现。** 这不是流程洁癖，而是防止 agent 自我验证、删测试、绕过设计文档。

### 40.3 工具栈：不要泛称“Claude Agent SDK”，改成可落地组合

| 层 | 推荐 | 备注 |
|---|---|---|
| 主开发 agent | **Claude Code subagents** | 支持自定义 subagent、模型选择、工具 allowlist / denylist、权限模式 |
| 辅助代码 agent | **OpenAI Codex CLI** | 适合 TS / Python / repo-scale patch；用在独立 task，不和 Claude 同时写同一文件 |
| 编排 | Custom Python + Redis / SQLite queue | 开发流水线不用 LangGraph；LangGraph 只用于产品的 `MealAnalysisGraph` |
| PR / CI | GitHub Actions + branch protection + CODEOWNERS | 让权限规则真正生效 |
| Observability | LangSmith / Helicone + structured logs | 追踪 agent 决策、LLM 成本、latency、失败循环 |
| Cost control | Helicone rate limit / cost alert + provider hard limit | Cost Watcher 不是 LLM，它是硬策略 |
| 人机沟通 | Slack App / GitHub Issues | webhook 只做通知；需要按钮审批时用 Slack App / Socket Mode |

### 40.4 Path ACL：用文件权限 + CI 双重实现

单靠 prompt 说“Developer 不能改 tests”不够。要让 CI 检查实际 diff。

```yaml
# dev_agents/policies/path_acl.yaml
roles:
  developer:
    allow:
      - "services/**"
      - "apps/**"
      - "src/**"
    deny:
      - "tests/**"
      - "evals/golden_set/**"
      - "prompts/**"
      - "DESIGN_zh*.md"

  tester:
    allow:
      - "tests/**"
      - "evals/**"
    deny:
      - "services/**"
      - "apps/**"
      - "src/**"
      - "prompts/**"
      - "DESIGN_zh*.md"

  reviewer:
    allow: []
    comment_only: true

  doc:
    allow:
      - "README.md"
      - "CHANGELOG.md"
      - "docs/**"
    deny:
      - "src/**"
      - "services/**"
      - "apps/**"
      - "tests/**"
      - "evals/golden_set/**"
      - "prompts/**"
```

CI 增加一步：

```bash
python dev_agents/policies/check_acl.py \
  --role "$AGENT_ROLE" \
  --base origin/main \
  --head HEAD
```

如果 Developer PR 改了 `tests/**`，CI 直接 fail。这样 agent 不能通过删测试让自己通过。

### 40.5 CODEOWNERS 与 branch protection

```text
# .github/CODEOWNERS
/DESIGN_zh*.md              @human-owner
/prompts/**                 @human-owner @reviewer-agent
/evals/golden_set/**         @human-owner
/tests/**                   @tester-agent @human-owner
/services/**                @reviewer-agent
/apps/**                    @reviewer-agent
/docs/adr/**                @architect-agent @human-owner
```

Branch protection：

```text
main branch rules:
  ✓ Require pull request before merging
  ✓ Require approving review
  ✓ Require review from Code Owners
  ✓ Require status checks:
      - backend / lint
      - backend / typecheck
      - backend / tests
      - eval / golden_set_replay
      - frontend / build
      - security / path_acl
  ✓ Block force push
  ✓ Restrict who can dismiss reviews
```

### 40.6 Cost Watcher：从“提醒”升级为“硬停机”

```yaml
budgets:
  daily_total_cap_usd: 25       # 第 1 周先保守，跑稳后再调到 50
  planner_agent: 3
  architect_agent: 6
  developer_agent: 10
  tester_agent: 4
  bug_fixer_agent: 5
  reviewer_agent: 6
  doc_agent: 1

hard_stops:
  - daily_total_cap_exceeded
  - same_task_retry_count > 3
  - same_file_modified_by_agent_count_24h > 5
  - similar_diff_loop_detected
  - eval_regression_over_threshold
```

策略：

- **Cost Watcher 不应该是一个会“商量”的 LLM。** 它应该是 gateway / cron / script。
- 达到 80% 预算：Slack 警告。
- 达到 100%：停止新任务，只允许 Doc Agent 整理状态。
- 达到 120%：全部 agent 停止，等待人工确认。

### 40.7 Loop detection

```python
def should_escalate(task_run: TaskRun) -> bool:
    if task_run.bugfix_attempts >= 3:
        return True
    if task_run.same_file_modified_count_24h >= 5:
        return True
    if task_run.diff_similarity_last_3 > 0.82 and task_run.tests_still_fail:
        return True
    if task_run.cost_usd > task_run.budget_usd:
        return True
    return False
```

升级信息必须包含：

```text
- task_id
- 当前失败命令
- 最近 3 次 diff 摘要
- 失败日志最后 80 行
- agent 自己认为卡住的原因
- 建议你做的 1 个决定
```

不要让 agent 给你 3000 行日志。它必须把问题压缩成一个可拍板的选择。

### 40.8 Agent Brief 模板

每个 task 进入队列前必须生成 brief：

```markdown
# Agent Brief: TASK-013 Interval Macro Calculator

## Goal
Implement deterministic kcal/protein range calculation from portion range × macro entry.

## User value
This powers MacroAgent's honest accounting output: best estimate + range.

## Allowed files
- services/tools/src/macro_calculator.py
- tests/tools/test_macro_calculator.py  # only if role=tester

## Disallowed files
- prompts/**
- DESIGN_zh*.md
- evals/golden_set/**

## Acceptance
- Supports grams range and kcal/protein per 100g
- Returns kcal_best, kcal_range, protein_best, protein_range
- Handles missing protein as 0 with warning
- 100% unit test coverage for this module

## Commands
- pytest tests/tools/test_macro_calculator.py
- ruff check services/tools/src/macro_calculator.py

## Budget
- max_tokens: 40000
- max_cost_usd: 1.50
- max_attempts: 2

## Escalate if
- Requires changing data model
- Needs prompt changes
- Ambiguous formula for best estimate
```

### 40.9 推荐 repo 增量结构

```text
macroagent/
  .claude/
    agents/
      planner.md
      architect.md
      developer.md
      tester.md
      bug_fixer.md
      reviewer.md
      doc.md

  .codex/
    AGENTS.md
    config.toml.example

  dev_agents/
    orchestrator.py
    queue.py
    cost_watcher.py
    loop_detector.py
    daily_report.py
    policies/
      path_acl.yaml
      check_acl.py
    briefs/
      TASK-001.md
    reports/
      daily_2026_05_03.md
    templates/
      agent_brief.md
      adr.md
      pr_review_checklist.md

  docs/
    adr/
      ADR-001-local-first-vs-cloud-demo.md
      ADR-002-langgraph-only-for-meal-analysis.md
      ADR-003-agent-dev-pipeline-acl.md
```

### 40.10 每日工作流：15–30 分钟是目标，不是 Day 1 现实

启动阶段前 3 天，你每天可能需要 60–90 分钟，因为要修 ACL、CI、repo 结构和 agent brief 模板。等流水线稳定后，才进入 15–30 分钟节奏。

```text
09:00  看 Daily Report
09:05  处理 escalations：只拍板，不亲自重写全部代码
09:15  看昨天 merged PR：重点看 prompts、eval、schema、safety
09:25  决定今天最多 5 个 task 入队
19:00  可选 spot check
周五 17:00  强制真机 / 端到端完整 pass
```

### 40.11 真机门禁

任何涉及 iOS / 相机 / HealthKit / ARKit / LiDAR 的任务，都必须过真机门禁：

```text
必须真人验证：
  - 相机权限弹窗
  - 照片方向 / HEIC / EXIF
  - 暗光环境
  - 多设备屏幕尺寸
  - HealthKit 权限链
  - LiDAR / depth 是否真的可用
  - 断网 / 弱网
  - 后台恢复
```

没有真机 pass，不开下一个移动端 EPIC。

### 40.12 开发 pipeline 为什么不用 LangGraph

产品里的 `MealAnalysisGraph` 用 LangGraph 是对的，因为它有用户等待、thread_id、HITL、状态恢复。

开发流水线不用 LangGraph，是因为它更像 CI/CD + queue + policy engine：

| 需求 | 更适合的实现 |
|---|---|
| task queue | Redis / SQLite queue |
| ACL | YAML policy + CI check |
| cost cap | Helicone / gateway / cron |
| retry limit | Python state machine |
| PR review | GitHub API |
| daily report | GitHub Issues / Slack |

一句话：

> **产品 agent 用 LangGraph；开发 agent 用队列 + 策略 + CI。不要把所有“有 agent 的东西”都塞进同一个框架。**

### 40.13 面试中怎么讲这套 pipeline

30 秒版本：

> “我没有把它设计成完全无人监督的 agent 工厂，而是 human-supervised autonomous pipeline。Planner 拆任务，Developer 写代码，Tester 独立拥有 tests，Reviewer 只能评论不能改 source，Cost Watcher 是硬停机策略。所有 PR 必须过 branch protection、CODEOWNERS、path ACL 和 eval gate。这样既能让 agent 帮我压缩重复工程时间，又不会让它通过删测试、改 prompt、污染 golden set 来伪造进展。”

60 秒版本：

> “这套系统的核心不是 7 个 agent，而是 4 个隔离：代码和测试隔离、实现和评审隔离、产品 prompt 和普通 source 隔离、自动开发和人类拍板隔离。AI coding 真正危险的地方不是写不出代码，而是写出看起来合理、但绕过测试和产品目标的代码。所以我把 Developer 限制在 source，Tester 限制在 tests，Reviewer comment-only，prompt 和 golden set 只允许我改。它本质上是一个带 AI worker 的 CI/CD 系统，而不是一个放任 agent 自己规划产品的黑盒。”

---

## 四十一、把“自主开发”写进路线图时的措辞修正

### 41.1 不建议写法

```text
Agent 团队 24/7 跑，30 天等于 720 工程小时，能自主产出完整产品。
```

问题：

- “工程小时”不等于有效产出。
- 容易让人质疑你低估集成成本。
- 健康类产品需要真实用户和真机测试，不能靠 agent 自证完成。

### 41.2 建议写法

```text
MacroAgent uses a human-supervised multi-agent development pipeline to compress implementation, testing, and documentation cycles. Agents can generate PRs, run eval gates, fix bounded failures, and produce daily reports, while product bets, prompts, golden sets, safety policy, and real-device validation remain human-owned.
```

中文版本：

```text
MacroAgent 使用人类监督的多 Agent 开发流水线来压缩实现、测试和文档周期。Agent 可以生成 PR、运行 eval gate、修复有边界的失败并输出日报；但产品赌注、prompt、golden set、安全策略和真机验证仍由人类负责。
```

### 41.3 简历 / README 可用 bullet

```text
Designed a human-supervised autonomous development pipeline with specialized Planner, Developer, Tester, Reviewer, Cost Watcher, and Doc agents. Enforced path-level ACLs, branch protection, CODEOWNERS, eval gates, and cost caps to prevent self-validation and runaway LLM spend.
```

```text
Separated AI coding responsibilities into source-writing, test-writing, review-only, and documentation roles; blocked Developer agents from modifying tests/prompts/golden sets via CI path ACL checks and GitHub branch protection.
```

```text
Built agent operations controls including loop detection, max retry limits, daily cost budgets, Slack/GitHub escalation reports, and human checkpoints for prompts, safety policy, golden data, and real-device iOS validation.
```

### 41.4 当前最小可实现版本（1 天内）

不要一开始就做完整 agent 平台。第一天只做这个：

```text
1. .claude/agents/developer.md
2. .claude/agents/tester.md
3. .claude/agents/reviewer.md
4. dev_agents/policies/path_acl.yaml
5. dev_agents/policies/check_acl.py
6. .github/workflows/agent-guardrails.yml
7. docs/adr/ADR-003-agent-dev-pipeline-acl.md
```

先证明两个东西：

- Developer 改 `tests/**` 会被 CI 拦住。
- Reviewer 只能输出 review，不直接改 source。

这两个成立，再加 Planner、Cost Watcher、Doc Agent。否则流水线看起来很宏大，但核心安全机制没落地。


---

## 参考资料（v0.8–v1.0.1 技术补强）

- LangGraph 官方文档：低层级 agent orchestration，支持 durable execution、human-in-the-loop、memory 等能力：https://docs.langchain.com/oss/python/langgraph/overview
- LangGraph durable execution 官方文档：保存工作流进度，支持 human-in-the-loop 和失败后恢复：https://docs.langchain.com/oss/python/langgraph/durable-execution
- LangChain structured output 官方文档：通过 schema / Pydantic / tool strategy 获取结构化输出：https://docs.langchain.com/oss/python/langchain/structured-output
- LangSmith 官方文档：observability、evaluation、deployment、platform setup 的集成工作流：https://docs.langchain.com/langsmith/home
- GitHub Actions 官方文档：CI/CD 平台，可自动化 build、test、deploy pipeline：https://docs.github.com/en/actions/get-started/understand-github-actions
- GitHub Actions CI 官方文档：CI workflow 可在 GitHub-hosted 或 self-hosted runners 上 build/test：https://docs.github.com/en/actions/get-started/continuous-integration
- AWS Lambda 官方文档：Lambda 支持 container images 和 zip archives 作为部署包：https://docs.aws.amazon.com/lambda/latest/dg/images-create.html

- Claude Code subagents 官方文档：支持自定义 subagent、工具限制、权限模式和模型选择：https://docs.anthropic.com/en/docs/claude-code/sub-agents
- Claude prompt caching 官方文档：工具、system message、messages 等可缓存，适合降低重复 agent 调用成本：https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching
- OpenAI Codex CLI 官方说明：Codex CLI 是本地运行的 coding agent：https://github.com/openai/codex
- OpenAI Codex 配置参考：config.toml / requirements.toml 配置：https://developers.openai.com/codex/config-reference
- GitHub branch protection 官方文档：可要求 PR review 和 required status checks：https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/managing-a-branch-protection-rule
- GitHub CODEOWNERS 官方文档：可配合 branch protection 要求 code owner review：https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners
- GitHub Actions secure use 官方文档：自动化创建或批准 PR 可能带来安全风险，应限制权限：https://docs.github.com/en/actions/reference/security/secure-use
- Helicone custom rate limits 官方文档：支持按请求、成本或自定义属性限流：https://docs.helicone.ai/features/advanced-usage/custom-rate-limits
- Helicone cost tracking 官方文档：支持成本阈值、告警和缓存策略：https://docs.helicone.ai/guides/cookbooks/cost-tracking
- Slack Socket Mode 官方文档：无需公开 HTTP endpoint 即可使用 Events API 和交互功能：https://api.slack.com/apis/socket-mode
